# Claude-5 brief — the implementation plan

Derived 2026-09-03 by a fifteen-agent audit that re-derived all 78 claims in the owner's
context-engineering brief against the working tree, then re-judged every verdict under the owner's
correction that researched guidance is to be built rather than audited for staleness. The
recalibration moved `needs-ruling` from 11 items to 1 and `actionable` from 6 to 18; eleven verdicts
were overturned between the two passes.

Companion to [`summoner-context-engineering-2026-09-03.md`](./summoner-context-engineering-2026-09-03.md),
which holds the programme's rules, rulings, dispatch log and corrections ledger. **That file is
canonical for what happened; this one is canonical for what is left.**

Every figure below was measured with the command beside it. Re-derive before trusting any of them —
the tree has moved under this plan already.

---

# Implementation Plan — the Claude‑5 context‑engineering brief

## 1. What gets built

Ordered so doctrine lands before the agents that cite it, the render path before the schemas that write through it, and the guards last. Every "not yet done" command was run against the working tree on 2026‑09‑03 at HEAD `0a002961`.

### A. Doctrine

**A1 — prompt‑bible: every figure lives once, in the table.** _(This is the "delete unsourced percentages" item and the "revise Technique #4 to structure, not accuracy multiplier" item; they are one edit and are merged here.)_
`packages/cli/.ai-docs/standards/prompt-bible.md`. Delete the five "Impact & Evidence" bullets that restate a Performance Metrics row away from its Evidence Source column — lines 130 (`80%+ reduction in hallucination`), 245 and 246 (the XML‑tag pair, whose removal is what completes Technique #4), 305 (`Up to 30% improvement`), 419 (`74.4% on SWE-bench Verified`). Line 307 (`Achieved 90% accuracy with query-last`) has no table row at all — delete it. Line 20's `72.7%+ on SWE-bench` sits in the Purpose paragraph with a benchmark named and no source; drop the figure, keep the sentence. Leave rows 29/31/32/34 alone: the table is the only place the Conclusion (line ~1166) allows a figure to live.
Not done: `grep -n '[0-9]\+\(\.[0-9]\+\)\?%' packages/cli/.ai-docs/standards/prompt-bible.md` returns 11 lines; it should return the four table rows.

**A2 — prompt‑bible: Technique #10 becomes the progressive‑disclosure treatment.**
Same file, line 518. Rename the heading to `Technique #10: Progressive Disclosure — Just-in-Time Context Loading` and extend the body with the principle that §5's Weight checklist (lines 904‑909) currently checks without naming: what stays in the prompt is what the role needs in order to decide; detail needed occasionally arrives through a skill the agent invokes or a file it reads; reference material the agent could look up does not go in at all. **Keep the existing Application paragraph verbatim underneath** — it is the correct narrower ruling about what an agent's own files carry, and the expansion must not overwrite it. **Do not renumber to #2**: that shifts #2–#9 and breaks five internal cross‑references plus `documentation-bible.md:594`. Technique #10 itself has zero inbound cross‑references, so the rename is free. Describe the lazy‑load pattern without citing a skill — the five methodology partials are not skills, and only `meta-methodology-research-methodology` exists in `SKILL_IDS`.
Not done: `grep -n -i 'progressive disclosure' packages/cli/.ai-docs/standards/prompt-bible.md` returns one line, 14, inside the authority block.

**A3 — skill‑atomicity‑bible: the Description Contract.** The highest‑value item in the plan.
`packages/cli/.ai-docs/standards/skill-atomicity-bible.md`. Under "SKILL.md Content Standard", beside the budget paragraph at line 115, add a subsection stating: the frontmatter `description` is the only text loaded before the skill fires and is therefore the whole basis of the firing decision; it must name what the skill covers and then the conditions that should load it — the shape `skill-summoner/output.md:67` already prescribes; `usageGuidance` in `metadata.yaml` is catalogue copy for the picker and is never loaded by the model, so a "when" clause living only there does not make the skill fire; body Auto‑detection keywords are read only after firing. Amend the table row at line 97 to require that content rather than the field's presence. Amend line 102 (`Auto-detection | Keywords that trigger this skill`) so it stops promising a trigger those keywords cannot deliver. Add the matching checkable row beside line 628. Do not restate in the primer — it delegates structure to the bible at lines 32 and 69 by design.
Not done: `grep -n -i 'description' packages/cli/.ai-docs/standards/skill-atomicity-bible.md` returns only 97 and 628 plus table column headers; `grep -n -i 'description\|frontmatter' packages/cli/.ai-docs/standards/skill-atomicity-primer.md` returns nothing.

**A4 — skill‑atomicity‑bible: the budget is denominated in tokens.**
Same file. Replace `**Target: ~500 lines.**` at line 115, and mirror the change in the four derived sites — lines 771 (`core.md target size | 200-500 lines`), 774, 868, 878 — so the file does not carry both currencies. Tokens are what cost; lines are a poor proxy at a 39.0 median chars/line. The number is the one item in section 5. Whichever number lands, keep a `wc -l` proxy beside it so the rule stays checkable by an agent that holds no tokenizer.
Not done: `grep -n token packages/cli/.ai-docs/standards/skill-atomicity-bible.md packages/cli/.ai-docs/standards/skill-atomicity-primer.md` returns only "design tokens".

**A5 — skill‑summoner: the size bound becomes a hard output requirement.**
`packages/cli/src/agents/meta/skill-summoner/output.md`, inside `<output_format>` in the `### SKILL.md` section, immediately before the ` ````markdown ` fence at line 64. One paragraph: the file has a size bound and it is a requirement of the output rather than advice; past it, move full code examples into `examples/` — extraction rather than deletion, since concepts, decisions and red flags stay; check it with `wc -l <skill-dir>/SKILL.md` before reporting the file, and where the count is over, say so and name what was moved. State it in the same unit A4 settles on, with `wc -l` as the check — skill‑summoner's tools are Read/Write/Edit/Grep/Glob/WebSearch/WebFetch/Bash, so `wc` is reachable and a tokenizer is not.
Not done: `grep -rn '500 lines\|token\|budget' packages/cli/src/agents/meta/skill-summoner/output.md` returns nothing; the only size guidance in that agent is soft prose at `playbook.md:30`.

**A6 — agent‑summoner: a size bound for the files it generates.**
`packages/cli/src/agents/meta/agent-summoner/playbook.md`, inside `<content_placement>`, appended after the "Moving content beats choosing between keeping it and dropping it" paragraph — that paragraph says to move content and never says when. State it in bytes with a census command rather than a figure, following §4's precedent of shipping the command: a generated agent's five files together sit around 20 KB, the playbook is the file that runs away (median 9,851 B, worst 35,420 B, a 7× range), and where a partial is more than about twice the roster median for that file the surplus is depth a task should reach for. `wc -c src/agents/*/*/playbook.md | sort -n` is the roster; `meta/agent-summoner` and `meta/skill-summoner` are the worked sizes.
Not done: `grep -rni 'budget\|token\|500 lines\|wc -c' packages/cli/src/agents/meta/agent-summoner/*.md` returns one hit, `playbook.md:232`, about skill preloading.

### B. Hooks

**B1 — the gate composes instead of being replaced. Prerequisite for B3 and for B4's caveat.**
`packages/compile/src/agent-source.ts` and `packages/cli/src/agents/_templates/agent.liquid`. Today lines 11‑12 of the template are an exclusive `{% if agent.hooks %}` / `{% elsif tools contains Write or Edit %}` pair, so any declared hook silently deletes the completion gate. Build the effective hooks record in `renderAgent`'s data path before render: start from the gate entry when `tools` contains Write or Edit, then merge the declared record per event — a declared `SubagentStop` replaces the gate's entry (a project's gate is its own to state, which is the existing rule), every other event is added. The template collapses to a single `{% if agent.hooks %}hooks: {{ agent.hooks | json }}{% endif %}`. Output is byte‑identical whenever nothing declares hooks, so no compiled agent moves. `sanitizeHooks` already runs on whatever reaches `agent.hooks`, whatever its origin, so sanitisation is free.
Not done, and untested either way: `grep -rln 'if-present' packages/cli packages/compile apps` hits only docs, `agent-source.ts` and build output — no test file. Write that test red first; it closes an untested invariant as well as enabling B3. Reproduction of the current break: render `agent.liquid` through liquidjs with `tools: [Read, Write, Edit]` plus a declared `PostToolUse` hook — the emitted `hooks:` carries only `PostToolUse` and the gate is gone.

**B2 — confirm the emitted event key, and hand‑run the gate once.**
Same files. `code.claude.com/docs/en/sub-agents` documents the subagent‑frontmatter hooks table as `PreToolUse` / `PostToolUse` / `Stop`, with "`Stop` … (converted to `SubagentStop` at runtime)", and adds that frontmatter hooks also fire when the agent runs as the main session via `--agent`, where `SubagentStop` never fires and `Stop` does. `Stop` is therefore correct in both modes and `SubagentStop` in at most one. A literal `SubagentStop` may still work ("All hook events are supported"), but nothing in this tree has ever observed the gate fire — `~/.claude/debug` holds only `latest`, and the programme log records the hand‑run as unreachable. Rule 5 of the owner's five rests entirely on a mechanism nobody has watched run, so this item is: run a writing sub‑agent against a project with a failing `typecheck`, confirm the block, and emit whichever key actually fires.

**B3 — the `PostToolUse` reference hook on `Edit|Write`.** Lands after B1.
The vendor's worked example on the same page is exactly this hook. `PostToolUse` fires after the write has committed, so exit 2 showing stderr to Claude is the documented mechanism rather than a limitation. Scope it to lint plus format on the single file written — measured on this machine, `npx eslint src/cli/lib/schemas.ts` is 2.1–2.3 s and `npx prettier --check src/cli/lib/schemas.ts` is 0.26–0.32 s, against ~14 s for package‑wide `format:check` and ~52 s for the gate command; a project‑wide `typecheck` per edit is not affordable. Formatting is the class the gate cannot see at all: `COMPLETION_GATE_COMMAND` runs `typecheck` and `lint`, and in `packages/cli` formatting lives in a separate `format:check` script. Carry the gate's own inertness guard (`command -v … || exit 0`, `--if-present`) because a compiled agent installs into someone else's repository. Then enable it in this repository through `~/.claude-src/config.ts` once B4 lands — that is CLI‑715's acceptance criterion 2, and this is that row's first slice rather than a new item.
Not done: `grep -rn '^hooks:' packages/cli/src/agents/*/*/metadata.yaml packages/cli/.claude-src/agents/*/*/metadata.yaml` returns nothing; no `.claude/settings.json` exists in this repo; the 9 of 13 files in `~/.claude/agents/` that carry a `hooks:` line all carry the gate alone.
Document in the same diff: `hooks` is "Ignored for plugin subagents", and `agent-recompiler.ts:209` writes compiled agents into `getPluginAgentsDir(pluginDir)`, so in a plugin‑mode install the emitted gate is already inert today.

**B4 — `hooks` overridable per project, the same chain as `model`.**
Failing probe first: feed `projectConfigLoaderSchema` an agent entry carrying `model`, `effort` and `hooks`; it returns `success: true` and the parsed entry is `{name, scope, model, effort}` — hooks is silently stripped, because the inner `z.object` is default‑strip while the outer is `.passthrough()`. Then, in order:

1. `packages/cli/src/cli/types/config.ts` — `hooks?: Record<string, AgentHookDefinition[]>` on `AgentScopeConfig` (after `effort`, line 46) and on `CompileAgentConfig` (line 56). `AgentHookDefinition` is already declared at `types/agents.ts:28`.
2. `packages/cli/src/cli/lib/schemas.ts` — `hooks: strictHooksRecordSchema.exactOptional()` in the `projectConfigFields.agents[]` object after `effort` (line 296). That schema already exists at lines 99‑102 and is what `agentYamlGenerationSchema` uses.
3. `packages/cli/src/cli/lib/installation/local-installer.ts` — the `tuning` object at line 369 gains `...(agentConfig.hooks !== undefined && { hooks: agentConfig.hooks })`.
4. `packages/cli/src/cli/lib/resolver.ts` — hoist `const hooks = agentConfig.hooks ?? definition.hooks` beside the model/effort lines at 117‑118 and spread it conditionally at line 139. The conditional spread **must** stay: the docblock two lines above records that an explicit `undefined` sends `{% if agent.hooks %}` down the wrong branch.
5. `packages/compile/src/config-types-source.ts` — this string becomes the consumer's `.claude-src/config-types.ts`, which their `config.ts` is typed against; without it a `hooks` key is a type error in the very file it is meant to be written in. Emit `AgentHookAction`/`AgentHookDefinition` and add the field to the emitted `AgentScopeConfig` (line 88).
6. `packages/compile/src/config-source.ts:128` — `"hooks"` into `CANONICAL_AGENT_ENTRY_ORDER` between `effort` and `excluded`, or the writer emits it unordered after every named key.
7. `packages/compile/src/contract/emission-scenarios.ts` — six golden copies of the emitted `AgentScopeConfig` block redden and must be updated.
8. `packages/cli/src/cli/lib/seed/config-to-seed.ts` — **the step that would otherwise ship a silent bug.** This module refuses what the wire cannot express rather than dropping it (`unnameableModelMessage` at line 82, `unwritablePairMessage`), and its docblock says "nothing here leans on a wire default the decoder would have to guess back". `toSeedAgent` (line 208) spreads only `on`/`scope`/`model`/`effort`, and `seedAgentSchema` in `packages/matrix/src/seed.ts:235` has no hooks field. Add a refusal collecting agents that carry `hooks`, worded like `unnameableModelMessage`. Do not widen the seed contract instead — `grep -rn hooks apps/editor/src` returns one skill‑icon string, so nothing on the wire would ever produce one.
9. `bun run generate:matrix` from `packages/cli`, re‑vendoring `packages/matrix/src/vendor/config.ts` (identical today, so nothing is satisfied there either).

**Do not** touch `project-config.schema.json`: `packages/cli/scripts/generate-json-schemas.ts` states in its header that it and `project-source-config.schema.json` are hand‑maintained and "neither emitted nor judged here", and its `properties.agents` is currently `{type: array, items: {type: string}}` — it does not describe today's object entries at all, so adding a hooks property would decorate a node that is already wrong.

**B5 — `hooks-bible.md`, scoped to what nothing in this tree owns.**
New file `packages/cli/.ai-docs/standards/hooks-bible.md`, plus a row in the standards table of `packages/cli/.ai-docs/DOCUMENTATION_MAP.md` (after line 147) — the documentation bible requires a row when a document is added. It **owns** the vendor contract: the "Exit code 2 behavior per event" table transcribed from `code.claude.com/docs/en/hooks` with the URL and a verified‑against date beside it; the exit‑0 companion rule (stderr from a hook that exits 0 goes to the debug log only and Claude never sees it, which is why an observe‑only hook must exit 2 to be heard); the tighten‑never‑loosen rule in the vendor's own words ("The hook can deny the call, but staying silent doesn't approve it", and on exit 2 "even a JSON `permissionDecision` of `\"allow\"` can't override it"); the supported subagent‑frontmatter shape; and the plugin‑subagent caveat. It **restates nothing** product‑side: it points at `compilation-pipeline.md` § the emitted frontmatter, `agent-system.md` § The Emitted Completion Gate, and `prompt-bible.md` Technique #1, which is the compliant pattern `agent-system.md` already demonstrates.
Carry this correction into the file rather than the brief's list: the brief names PreToolUse, Stop, SubagentStop and PreCompact as blockers and PostToolUse and SessionStart as observe‑only. The docs mark PreToolUse, Stop and SubagentStop as blocking and PostToolUse as "No — Shows stderr to Claude; the tool already ran", but **PreCompact and SessionStart do not appear in the exit‑2 table at all**, and the table additionally marks Yes for UserPromptSubmit, UserPromptExpansion, TeammateIdle, TaskCreated, TaskCompleted, ConfigChange and PostToolBatch. There are 32 documented events. Re‑fetch rather than writing any of it from memory — the one prior attempt to write this contract from memory put `stop_hook_active` into a design, and the docs do not carry it.
Not done: `ls packages/cli/.ai-docs/standards/` returns twelve entries and no hooks document; `grep -rn 'PreToolUse\|PreCompact\|SessionStart\|UserPromptSubmit' --include='*.md' packages apps todo` (excluding node_modules and dist) returns one hit, the programme's own plan file.

### C. The skill description pipeline

**C1 — the `Use when:` payload, and the mirror the in‑flight fix must not miss.**
The CLI‑872(a) fix at `packages/cli/src/cli/lib/stacks/stacks-loader.ts:251` (`usage: \`when working with ${category}\``) is in flight and is not re‑dispatched here. Two facts the lane holding it needs. First, it is a two‑line change, not plumbing: `stacks-loader.ts`already imports`matrix`from`matrix-provider`, and `usageGuidance?: string`is on`SkillCore`and therefore on`matrix.skills[id]`—`SkillDefinition`need not grow a field. Second, **there is a byte‑identical second copy of the placeholder** at`apps/editor/src/features/configure/lib/output-preview.ts:320`, whose own docblock says it mirrors `resolveAgentConfigToSkills`"including the usage line it derives from the category key". Land one without the other and the editor preview stops matching what install writes.`matrix`is already in scope there.
Adjacent finding for the same lane, not fixed by it: the`- Description:`line already diverges between the two surfaces. The editor takes`described.description`from the matrix, where`generate-source-types.ts:164`sets`description: metadata.cliDescription`; the CLI takes it from SKILL.md frontmatter at `loader.ts:308`. 228 of 238 skills differ on those two fields.
Not done: `grep -rn 'usage:' packages/cli/src --include='_.ts' | grep -v test`returns exactly one producer,`stacks-loader.ts:251`; `grep -h '^- Use when:' ~/.claude/agents/_.md | grep -v 'when working with'` returns nothing across 232 lines in 13 agents.

**C2 — the scaffold emits a SKILL.md description that carries its trigger.**
`packages/cli/src/cli/lib/marketplace-scaffold.ts`. `skillMd()` (line ~213) writes `description: A placeholder skill — replace it with one of your own` — 51 characters, no condition — while `metadataYaml()` two functions below writes `usageGuidance: "Use when you need a worked example of this marketplace's skill shape"`, a proper trigger sentence in the field nothing reads. That is CLI‑872(b) reproduced inside code this repository owns, in the exemplar every new marketplace copies. Change the emitted description to carry the conditions, matching the `usageGuidance` written beside it. Add a test in `packages/cli/src/cli/lib/__tests__/` that renders the scaffold, parses both files' frontmatter, and asserts the description clears a length floor and contains a trigger clause (`/\bwhen\b/i` is the census's own operationalisation) — and asserts the same of `usageGuidance`, so the two cannot drift apart again.
**Do not** tighten `skillFrontmatterValidationSchema` (`schemas.ts:675`, `description: z.string().min(1)`): that gates a stranger's SKILL.md at install time, and 219 of our own 238 skills carry no trigger word in their description, so a trigger rule there would start refusing skills that install today, ours included.
Not done: the literal string `SKILL.md` never appears in `marketplace-scaffold.ts` (the name comes from `STANDARD_FILES.SKILL_MD`), which is why a `grep -rn "SKILL.md" packages/cli/src` misses it — use `grep -n 'SKILL_MD' packages/cli/src/cli/lib/marketplace-scaffold.ts`.

### D. The guards

**D1 — the shouting scan admits an interior numeral.**
`packages/cli/src/cli/lib/__tests__/agent-baseline-is-slim-and-positively-framed.test.ts`, the `SHOUTING` const. The literal the guard exists to catch — `DISPLAY ALL 5 CORE PRINCIPLES AT THE START OF EVERY RESPONSE`, at `agent.liquid` lines 37 and 159 at HEAD, retired in this uncommitted tree — is missed, because the numeral splits the capital run into two‑word halves and `{3,}` needs four. Replace `/\b[A-Z][A-Z']{2,}(?:\s+[A-Z][A-Z']{2,}){3,}\b/` with `/\b[A-Z][A-Z']{2,}(?:\s+(?:\d+\s+)?[A-Z][A-Z']{2,}){3,}\b/`: the digit is an interior separator, not a countable word, so four capitalised words are still required and a bare number cannot pad a three‑word run. Verified against the file's own constants — the new pattern catches both spellings of the literal and `ALWAYS RE-READ FILES AFTER EDITING`, and still misses "The CLI has 13 commands and 18 flags", "Use TypeScript 5 strict mode", "Run npm test 2 times". Update the const's docblock, which says "four or more consecutive capitalised words", and name the spelling that motivated the change so the next reader does not simplify it back.
Not done: `node -e` against the current const returns MISSED for the literal; all three `PROHIBITIONS` patterns also return false, since it is an imperative rather than a prohibition.

**D2 — a fixture that renders the dynamic‑skills block, so the guard can see it.**
Same file. `renderedBaseline()` passes `dynamicSkills: []`, so `{% if dynamicSkills.size > 0 %}` never fires and neither prohibition nor shouting scan has ever seen the one template block that historically carried the worst prose — at HEAD, lines 70‑130 of `agent.liquid` held "you MUST follow this three-step protocol", "COMPLETELY WORTHLESS", "LYING TO YOURSELF" and "MISS PATTERNS, VIOLATE CONVENTIONS, AND PRODUCE INFERIOR CODE"; the working tree's ten calm lines have no guard holding them there. Add `renderedWithDynamicSkills()` beside it — `createMockCompiledAgentData` already declares `dynamicSkills` and `preloadedSkillIds`, so it is a spread override — run the same two `offendingLines` assertions over it, and **assert the tag IS present when `dynamicSkills` is non‑empty**, which is the positive half owner ruling 2 protects. Leave `BASELINE_BYTE_BUDGET` on the empty‑content render; that measurement is deliberately the template's. While the file is open, add renders for the two `<skills_note>` branches at lines 63‑69, equally unscanned. Confirmed it would catch a revert: feeding the old block's lines through the file's own constants, two trip a guard, and both assertions are `toStrictEqual([])`.

### E. Consistency the 18/18‑opus move left behind

Not a model‑tier proposal — the tier is settled at 18 of 18. These are sites that still describe the old split, and one spec that is now vacuous by its own stated design.

- `packages/cli/e2e/fixtures/expected-values.ts:229` — `"api-tester": { defaultModel: "sonnet" }` → `"opus"`, and the docblock at 219‑224 loses its premise ("`api-tester` is the one whose default is NOT `opus`, which is why the default-preserving specs use it").
- `packages/cli/e2e/commands/init-from-scenarios-tuning.e2e.test.ts` — the comment at line 92 is false, and the two default‑preserving specs at 206 and 235 assert `opus` on an opus‑by‑default agent, which passes without the override arriving. The uniform fleet leaves no non‑opus probe; `effort` has no bundled default on any of the eighteen, so an effort‑preserving assertion (`noEffort: true`) is the replacement lever.
- `packages/cli/.ai-docs/reference/features/agent-system.md` lines 102, 148 and 152 (`**Model distribution:** 16 agents use opus, 2 use sonnet`).
- `apps/www/src/content/docs/docs/recipes/tune-an-agents-model.md:38` (`convention-keeper` and `api-tester` name `sonnet`) — this one is public.
  Not done: `grep -h '^model:' packages/cli/src/agents/*/*/metadata.yaml | sort | uniq -c` returns `18 model: opus`; the four sites above still say otherwise.

### F. The briefing correction

`packages/cli/CLAUDE.md`. The brief's closing safety claim — "every file you will edit is a source file… none of them are compile output" — is false three ways for `src/agents/`. Record the operational rule: run `bun run generate` in `packages/cli` after any edit to a `.liquid` template or an agent partial, then the three `:check` variants. And record the mirror that will never appear in `git status`: `packages/cli/.claude-src/agents/` is a gitignored 110‑file copy of `src/agents/` (`.gitignore:94`), it does not exist at HEAD, and `agent-recompiler.ts:157` merges project agents **over** built‑ins — so an editor who changes `src/agents/` and then runs `agents-inc compile` inside this repository to check their work compiles the old partials and sees no change. `diff -rq packages/cli/.claude-src/agents packages/cli/src/agents` (expected empty) is the last step of any agent‑partial edit. It matches right now only because this session repaired drift it had already caused.

---

## 2. Amendments our own documents need

Each is part of the work, not a gate.

- **`prompt-bible.md` Technique #1, Application (line ~101):** "**Declaring `hooks:` in `metadata.yaml` replaces that gate rather than adding to it**, so an agent that writes and states its own hooks states the gate among them, or loses it silently." After B1 this becomes _adds to_ — a declared `SubagentStop` replaces the gate's own entry, every other event composes with it. Land in the same diff as B1.
- **`packages/cli/src/agents/meta/agent-summoner/playbook.md:218`** carries the same sentence ("replaces the gate rather than adding to it") and changes identically. It is an agent partial, so it drifts `packages/compile/src/generated/corpus.ts` — regeneration is the orchestrator's, once, at the end.
- **`.ai-docs/reference/features/compilation-pipeline.md` § the emitted frontmatter:** the `{% if %}` / `{% elsif %}` pair description, and "**A declared `hooks` REPLACES the gate rather than adding to it**", become the compose description; gains a config‑level row after B4, and the plugin‑subagent caveat from B3.
- **`.ai-docs/reference/features/agent-system.md` § The Emitted Completion Gate:** the three‑state table row "declares `hooks` | its own hooks — **the declaration REPLACES the gate**" becomes a compose row, and "The branch is exclusive, so a project's gate is its own to state" is the sentence that has to move.
- **`prompt-bible.md` §5 Weight (lines 904‑909):** four checklist items that are progressive disclosure unnamed. After A2 they should point at Technique #10 rather than restate it.
- **`prompt-bible.md` §5 Frontmatter:** "`model` and `effort` match the work; a mechanical role does not need the largest model" now sits three sections below §4's ruling that this product's own eighteen are uniformly opus. The line is authoring guidance for a consuming project's agents and should say so, or a later pass reads it as licence to re‑optimise the fleet.
- **`skill-atomicity-bible.md:102`, "Auto-detection | Keywords that trigger this skill":** false, and it points authors at the wrong file. Those keywords live in the body (line 30 in `web-framework-react`, 28 in `api-framework-hono`), which is read only after the skill has fired. Amended by A3.
- **`skill-atomicity-primer.md:10`, "This primer outranks `skill-atomicity-bible.md`":** the description contract lands in the bible. Lines 32 and 69 already delegate structure and content ownership downward, so nothing needs adding — but if the primer is ever read alone, the contract is invisible.
- **`documentation-bible.md:202`, "A Count Lives in Exactly One Document… A second copy guarantees drift":** this constrains B5 rather than forbidding it. The hooks‑bible may own the vendor contract and must cross‑reference, never restate, the three existing product‑side owners. `agent-system.md`'s "**Owned by `compilation-pipeline.md` §…**" is the compliant pattern to copy.
- **`DOCUMENTATION_MAP.md` standards table (after line 147):** a `standards/hooks-bible.md` row, in the same diff as B5.
- **`last_validated: 2026-04-21`** on five standards — `prompt-bible.md`, `skill-atomicity-bible.md`, `skill-atomicity-primer.md`, `commit-protocol.md`, `loop-prompts-bible.md`. `prompt-bible.md` was rewritten today (443 insertions, 1,257 deletions) and still carries April. No checker enforces the field, so this is drift rather than a break; refresh the three this plan touches at minimum.
- **`todo/plans/summoner-context-engineering-2026-09-03.md:189`** — "`prompt-bible.md` already carries Technique #10, Positive Framing" describes a technique that no longer exists; it was folded into Technique #3.
- **Trackers, orchestrator‑only:** B3 is CLI‑715's first slice and folds into that row rather than opening a new one; C2 is the half of CLI‑872(b) that lives in this repository; the cross‑repo lint over 238 marketplace SKILL.md files stays CLI‑872(b) and is scoped separately. Sub‑agents do not edit `todo/`.

---

## 3. Already done

Nothing here needs a dispatch. All of it is uncommitted in this working tree, so it is done‑but‑unshipped — a revert brings every item back.

**prompt‑bible rewrite (Phase 1a).** Technique #1 Self‑Reminder Loop deleted — replaced not by a doc edit but by real infrastructure, `COMPLETION_GATE_COMMAND` at `agent-source.ts:404` wired at `:434`. Technique #3 Emphatic Repetition deleted, now "State Critical Rules Once, Plainly". Technique #11 "Think" Alternatives deleted, with §4 recording the retirement ("Write the word you mean"). Technique #2 compressed 45→35 lines, and the self‑restatement replaced by a pointer to the partial rather than a copy of it. Technique #5 kept at #5. Every percentage suffix gone from every heading (HEAD had five). §4 rewritten from "Sonnet 4.5 Specific Optimizations" to "Choosing the Model and the Effort", covering three live `metadata.yaml` keys. Positive Framing kept, but as a fold into Technique #3 rather than the promotion to slot #1 the brief asked for — the substance is present, the promotion is not, and slot #1 is now one of the owner's five rules.

**The dead reference (Phase 1b).** All five `claude-architecture-bible.md` citations removed from both summoners, and the corpus regenerated (2 stale copies at HEAD, 0 on disk). The brief offered create‑the‑file **or** remove‑the‑references; the second branch is what happened, and the structural contract the file would have carried is already in `prompt-bible.md` §2 "The Canonical Structure" and §5, which `agent-summoner/critical-requirements.md:4` now cites by full path.

**The skill size budget (Phase 1c).** `**Target: ~500 lines.**` predates this session and was already shipped at HEAD (line 119 there, 115 now), alongside the `reference.md`/`examples/` split at lines 48, 88, 656 and 658. The brief asked for a budget that already existed.

**The shared render path (Phase 2).** All six methodology partials staged as deleted and replaced by one 1,659 B `operating-principles.liquid`; `agent.liquid` rewritten. Both `DISPLAY ALL 5 CORE PRINCIPLES` occurrences and `ALWAYS RE-READ FILES AFTER EDITING` gone from template and from every compiled agent. `<core_principles>` gone entirely — replaced by `<operating_principles>` rendered from a partial, six principles rather than five, with principle 4 in positive form verbatim as the brief asked. All three sections the brief named as worth preserving survived, each compressed into one paragraph: "The Class Is the Unit of Repair", "The Specification Is a Claim" (including the Corrections‑is‑a‑required‑field rule and its "write nothing when nothing did" half), and "Name What Would Catch It". Measured in the no-skills posture — every content field empty, no skills — the baseline is the byte
count `agent-baseline-is-slim-and-positively-framed.test.ts` reports; re-derive it there rather
than carrying a figure, since the number depends on which template branch renders and an
unqualified count has already been wrong twice.

Since written: the mechanical half of "Verify by reading back" — re-read after editing — was cut from the baseline on 2026-09-03 on the owner's over-verification principle, once the gate gained `test`. The judgement half (name what would catch a violation) stays, folded into the paragraph on closing out a repair.

**The Skill tool grant.** Shipped at HEAD in 0.162.0 by a different mechanism than the brief assumed: no `metadata.yaml` names `Skill` at all, and `withSkillTool` in `packages/compile/src/agent-source.ts` appends it in code for both front doors. All thirteen compiled agents carry it, read‑only researchers and reviewer included.

**Hook infrastructure (Phase 4).** The brief predicted this grep would return nothing; it returns 31 hits across 14 files. `agent.liquid` emits `hooks:` from `metadata.yaml`; a `SubagentStop` gate is auto‑emitted for the 13 of 18 bundled agents holding Write or Edit; five agent‑shaped JSON schemas and six Zod schemas accept the key, two of them shipped at HEAD. Note the split: the schema half is at HEAD, the rendering half is uncommitted, so re‑deriving from `git show HEAD:` alone concludes the capability is absent.

**Both summoners (Phase 5).** `skill-summoner/playbook.md` 46,900 → 10,890 B; `agent-summoner/playbook.md` 45,884 → 14,929 B; the two agents' six files fell 66,197 → 23,803 and 60,820 → 21,736. Every string the brief asked to delete is already gone — the "all 13 prompt-bible techniques" audit trigger (which lived in agent‑summoner, not skill‑summoner as the brief said), the eight "Essential Techniques" occurrences, the "think"→consider/evaluate substitution trigger, and all 14 prompt‑bible citations in skill‑summoner (so there is nothing left to renumber). The description contract landed in `skill-summoner/output.md:67` — HEAD read `[One-line description]`, disk reads `[One line — what the skill covers, then the conditions that should load it]`. All four "what not to generate" rules are present in agent‑summoner's `<voice>` and `<content_placement>`, stated as instructions to the generator rather than doctrine.

**A guard for the retired forms (Phase 6).** `agent-baseline-is-slim-and-positively-framed.test.ts` exists, is staged, and passes 5/5. The brief's other half was false: no test anywhere asserts on the deleted strings, so there was nothing to update. Its empty‑content scoping is deliberate and correct — 17 of the 18 agent sources still carry the retired forms in their own files, so a scan across all compiled agents would fail today on every unmigrated one.

**Every figure in the brief's table reproduces at HEAD** — the shared scaffolding at ~4,500 tokens (18,033 B at 4 chars/token), the mean compiled agent at ~11,900 (47,244 B), 2,245 `You MUST` across 238 SKILL.md files, mean SKILL.md 4,130 tokens, 42 files over 500 lines, 16 of 18 on opus. Six of the eight are now stale because the cut the brief argued for has landed: scaffolding 3,948 B (−78 %), mean agent 29,613 B (−37 %), hooks from zero files to seventeen, models 18/18. The two that did not move are the two in the separate skills repository.

---

## 4. Genuinely blocked, with the mechanism

**Delete Technique #8, Post‑Action Reflection.** The stated ground is false: a `SubagentStop` hook fires once at stop, and post‑action reflection is a mid‑run pattern — different trigger, different failure class — and the gate runs only `typecheck` and `lint`, exiting 0 before doing anything in a project with no npm or no `package.json`. 18 of 18 agents ship a `<post_action_reflection>` block (19 files match, one being `agent-summoner/playbook.md`), and it also ships in the `.claude-src` mirror, the corpus and all 13 files in `~/.claude/agents/`. The blocks are role‑specific product content, not boilerplate — reviewer asks "Did every Should Fix pass the cost gate?", web‑tester asks "Do my tests fail for the RIGHT reasons?" — and they live in each agent's own `critical-reminders.md`, outside the baseline the byte budget measures, so there is no slimming prize. Deleting also falsifies the Performance Metrics footnote at line 41 ("Every row but this one is a technique in §1") unless row 36 goes too. **Amended approach:** compress the four generic questions; keep the technique, because the standard has to keep describing a block every shipped agent emits.

**Delete Technique #11, Write Verification.** Two reproductions. Tool grants: 13 of 18 agents hold Write or Edit and 5 do not — the four researchers and reviewer — and `agent.liquid` emits the gate only for agents that write, so those five get **no gate at all** and read‑back is their only cover. Scope: the gate runs `typecheck` and `lint`, neither of which opens a `.md` file, so `meta/codex-keeper` — whose whole job is editing `.ai-docs` — can report a doc edit that never landed and exit green. It is also the only documentation of a live rendered principle (`**Verify by reading back.**` in `operating-principles.liquid`, carried by all eighteen), the troubleshooting table points at it, and metrics row 39 is covered by the line‑41 footnote. **Amended approach:** none is needed — the technique costs no prompt bytes today, since its body already defers to the partial.

**Delete §4 in its entirety.** §4 is now "Choosing the Model and the Effort"; HEAD's five model‑specific headings are all gone, and the current §4 has no subsections. Its unions match `MODEL_NAMES` and `EFFORT_NAMES` member‑for‑member, its census returns 18 of 18, and it hosts the Technique #6 pointer at line 881 and item 4 of the Table of Contents at line 53. Decisively, it is the only prose home of the owner's 2026‑09‑03 all‑opus ruling and states it in the imperative — "Do not 'optimise' a shipped agent down a tier; that is the ruling, not an oversight." **Amended approach:** none needed; the deletion the brief describes already happened at the level of content, and executing the bullet literally now deletes the replacement.

**Delete Technique #6.** The model‑specific half is already gone — the heading was "Conditional Expansion Modifiers for Sonnet 4.5" at HEAD and is plain "Conditional Expansion Modifiers" now — so the brief's ground no longer describes the file. Three live cross‑references would break: §4's frontier‑model guidance at 874, the troubleshooting table at 974, and the delegation template's placeholder at 1084.

**A test asserting `<skill_activation_protocol>` is absent from compiled output.** The tag is live in the template and in all 13 compiled agents; the test would fail immediately against every real compile, and acting on it strips every agent's dynamic‑skill roster — the block is the only place a compiled agent learns a dynamic skill's plugin ref, and the `skills:` frontmatter key covers preloaded skills only. Settled by owner ruling 2. **Amended approach:** D2 — invert it.

**The `cliDescription + usageGuidance` join.** `cliDescription` is a wizard‑grid label capped at 60 characters; 228 of 238 skills have a SKILL.md description that is longer and more specific. `web-routing-react-router` is typical: `cliDescription` "Client-side and server-side routing for React applications" against the shipped description "Client-side routing with data APIs — loaders, actions, error boundaries, search params, nested layouts, and code splitting". The join replaces the second with the first in 228 files, and because the CLI loader sources every compiled agent's `- Description:` line from the same field, the degradation lands in every compiled agent too. **Amended approach:** `<existing description>. <usageGuidance>` — which is also what the authoring contract asks for, preserves the 228 hand‑written sentences, and stays under 1,024 chars in all 238 cases (median 289, max 849).

**Applying that join in the CLI build step.** Only the plugin path passes through `compileSkillPlugin`; `agents-inc eject` copies `src/skills/<id>/` verbatim, so an ejected skill would keep the bare description while the plugin‑installed copy of the same skill got the joined one — the same skill triggering differently depending on install mode, a divergence in the consumption direction the root CLAUDE.md treats as a real defect. The build step also feeds `plugin.json`'s description and the generated README from the same field. **Amended approach:** a one‑time scripted rewrite of the 238 source files in `agents-inc/skills` (CLI‑872(b), the owner's to scope), plus C2 here, which fixes the exemplar every new marketplace copies.

---

## 5. True owner choices

**One.** What number the SKILL.md budget takes when A4 converts it from lines to tokens.

- **~5,000 tokens** is the honest restatement of today's rule (500 lines × 39.0 chars/line ÷ ~3.6), preserves the existing 42‑file cleanup list, and happens to land on Anthropic's documented ceiling for a SKILL.md body. Cost: nothing changes in the corpus; the conversion buys clarity about what actually costs, not a cleanup.
- **~1,500 tokens**, the brief's figure, is unreachable as written: the smallest SKILL.md in the corpus is 7,141 characters, which needs >4.76 chars/token to fit, so 0 of 238 comply at any real tokenizer ratio. Adopting it means deleting rather than amending `skill-atomicity-bible.md:115` and `skill-summoner/playbook.md:30`, flagging all 238 skills on day one, and extracting roughly two‑thirds of every skill's body into `examples/` — a marketplace‑wide rewrite programme in `agents-inc/skills`, not a line in a standard. Cost: a 238‑row flag list carries no triage signal, and the 42‑row list stops existing the moment it is replaced.

Nothing else in the tally is a product choice. The two other items that wore that label are work: the hooks‑bible is a document someone has to write, and the progressive‑disclosure expansion is not in conflict with Technique #10's Application — that sentence rules on what an _agent's_ files carry, while the brief asks what the _standard_ teaches, and a standard can treat progressive disclosure fully while still telling authors most agents need no `<retrieval_strategy>` block.

---

## 6. Sequencing

Nine lanes. Files listed are owned exclusively — an agent needing a change in another lane's file reports the exact change rather than making it.

**Wave 1, fully parallel.**

| Lane                | Items               | Files owned                                                                                                                                                                                                                                                                                                                                                |
| ------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L1 doctrine‑prompt  | A1, A2              | `packages/cli/.ai-docs/standards/prompt-bible.md`                                                                                                                                                                                                                                                                                                          |
| L2 doctrine‑skills  | A3, A4, A5          | `.ai-docs/standards/skill-atomicity-bible.md`, `.ai-docs/standards/skill-atomicity-primer.md`, `src/agents/meta/skill-summoner/output.md`                                                                                                                                                                                                                  |
| L3 hooks‑render     | B1, B2, B3          | `packages/compile/src/agent-source.ts`, `src/agents/_templates/agent.liquid`, the new gate test, `.ai-docs/reference/features/compilation-pipeline.md`                                                                                                                                                                                                     |
| L4 hooks‑config     | B4 (all nine steps) | `src/cli/types/config.ts`, `src/cli/lib/schemas.ts`, `src/cli/lib/installation/local-installer.ts`, `src/cli/lib/resolver.ts`, `packages/compile/src/config-types-source.ts`, `packages/compile/src/config-source.ts`, `packages/compile/src/contract/emission-scenarios.ts`, `src/cli/lib/seed/config-to-seed.ts`, `packages/matrix/src/vendor/config.ts` |
| L5 scaffold         | C2                  | `src/cli/lib/marketplace-scaffold.ts` + its new test                                                                                                                                                                                                                                                                                                       |
| L7 guards           | D1, D2              | `src/cli/lib/__tests__/agent-baseline-is-slim-and-positively-framed.test.ts`                                                                                                                                                                                                                                                                               |
| L8 opus‑consistency | E                   | `e2e/fixtures/expected-values.ts`, `e2e/commands/init-from-scenarios-tuning.e2e.test.ts`, `.ai-docs/reference/features/agent-system.md`, `apps/www/src/content/docs/docs/recipes/tune-an-agents-model.md`                                                                                                                                                  |
| L9 briefing         | F                   | `packages/cli/CLAUDE.md`                                                                                                                                                                                                                                                                                                                                   |

**Wave 2, after L3 and L4 land.**

| Lane              | Items  | Files owned                                                                                                              |
| ----------------- | ------ | ------------------------------------------------------------------------------------------------------------------------ |
| L6 hooks doctrine | B5, A6 | `.ai-docs/standards/hooks-bible.md` (new), `.ai-docs/DOCUMENTATION_MAP.md`, `src/agents/meta/agent-summoner/playbook.md` |

L6 must follow L3 and L4 so its product‑side pointers describe the composed gate rather than the replacing one, and so the config‑level override exists to be pointed at. A6 rides in L6 because it is the only other edit to `agent-summoner/playbook.md`, whose line 218 is one of L3's amendments — one agent, one file, no contention.

**Collisions to hold.**

- **`src/cli/lib/resolver.ts`** is touched by L4 (line 139, hooks) and by the **in‑flight CLI‑872(a) fix** (line 27, `usage: ref.usage`). Different lines, same file. L4 must not start until that fix has landed, or the two must be one agent.
- **`apps/editor/src/features/configure/lib/output-preview.ts:320`** belongs to the in‑flight lane, not to any lane here. Whoever holds CLI‑872(a) lands both copies or the editor preview stops matching install.
- **`.ai-docs/reference/features/agent-system.md`** is owned by L8. L6 and L3 need its hook table changed and report the exact change rather than editing it.
- **`packages/compile/src/generated/corpus.ts`** is regenerated **once by the orchestrator**, after every agent‑partial edit has landed — L2's `output.md`, L6's `playbook.md`, and L3's template. `bun run generate` in `packages/cli`, then the three `:check` variants. Do not let a lane regenerate it; three lanes writing the same generated file is the conflict F exists to warn about.
- **`todo/`** is the orchestrator's: CLI‑715 absorbs B3, CLI‑872 loses its (a) half and keeps (b), and `ROADMAP.md` moves with them. Sub‑agents touch none of it.

**The one gate on the whole hooks half.** B2's hand‑run is the step the programme log records as never done. Until a writing sub‑agent has been watched blocking on a failing `typecheck`, everything B1–B5 asserts about the mechanism is documentation of an untested claim — run it before L6 writes the taxonomy down.

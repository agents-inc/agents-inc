# Owner's Worklist

## 1. Documentation that is wrong right now

### D1 — `SkillConfig.source` was renamed to `origin`; seven documents and seven comments never moved

**Executed and complete (2026-08-19).** This file is now a record of what was done rather than a
plan. The abbreviated finding names in each item's parentheses are **provenance**: they say which
findings motivated the work. Many of those files no longer exist — a finding is deleted as it closes
and its row in `agent-findings/INDEX.md` becomes the surviving record — so a name here that resolves
to nothing is the protocol working, not a broken reference.

_Findings: 08-16 marketplace-rename-stopped-at-typed-positions; 08-18 wizard-docs-still-describe-a-skillconfig-field._ **Effort: hour.**

- **Docs say:** `core-types.md` ("install source propagated from `SkillConfig.source`", "Derived at runtime from `SkillConfig.source` by `deriveInstallMode`"), `architecture-overview.md` ("`SkillConfig.source: string` … is the source of truth"), `boundary-map.md`, `utilities.md`, `features/seed-contract.md`. Same page: `core-types.md` says `SourceEntry` is "listed in `ProjectConfig.sources`".
- **Source does:** `src/cli/types/config.ts` declares `{ id; scope; origin; excluded? }`; `deriveInstallMode` reads `s.origin`; `ProjectConfig` has no `sources` field. `src/cli/lib/schemas.ts` declares `RENAMED_SKILL_ENTRY_FIELDS = { source: "origin" }` behind a `superRefine`, so **a config written from the documented shape hard-fails** with "the CLI does not read 'source' any more".
- **Fix:** `source` → `origin` in the five reference pages plus the comments in `types/matrix.ts`, `types/skills.ts`, `consts.ts`, `components/wizard/source-grid.tsx`, and `e2e/lifecycle/mixed-mode-skill-ref-format.e2e.test.ts` ("the canonical predicate"). Same pass: `wizard-store.ts`'s `resolveSkillRowInputs` JSDoc says `withSelectedSource` — the function is `withSelectedMode`; and `lib/wizard/scope-diff.ts`'s `agentSlotKey` JSDoc still cites `D-278`, the dangling ID that `documentation-bible.md` rule 3 names as the last offender in the tree.

This is first because it is the only entry where following the documentation produces a rejected config, not just a wasted read.

### D2 — Three user-facing CLI strings name things that do not exist

_Findings: 08-18 a-live-command-was-documented-as-deleted (×2); 04-21 eject-success-log-stale-partial-names._ **Effort: minutes.**

- `commands/compile.ts` refuses with "Add skills with `agents-inc add <skill>`". There is no `add` command — oclif exits 127 on the invocation the message hands the user. `reference/commands/index.md` step 7 quotes it faithfully, so both move together. Name `init` or `edit`.
- `commands/eject.ts` `--output` description claims "(default: .claude/ in current directory)". `resolveOutputBase` returns `.claude-src`, and the flag has no `default:` at all. `commands/index.md` is correct but has to warn readers "Read the table, not the help text" — the help text is the surface the user sees. Fix the flag, drop the warning.
- `commands/eject.ts` success log: "You can now customize templates, agent intro, workflow, and examples locally." The partials that ship are `identity.md`, `playbook.md`, `critical-requirements.md`, `critical-reminders.md`, `output.md`. None of the four nouns names a file the command wrote.

### D3 — `getScreen()` is documented as viewport-only in the two files that already say it isn't

_Finding: 07-31 getscreen-is-not-viewport-only._ **Effort: minutes.**

- **Docs say:** `e2e/helpers/terminal-session.ts` — `/** Reads the visible screen area (viewport only, no scrollback). */`; three member-table rows in `reference/testing/e2e-infrastructure.md` and two in `standards/e2e/page-objects.md` repeat it.
- **Source does:** `getScreen()` calls `readBufferLines(viewportY + rows)`, and `readBufferLines(n)` reads absolute lines `0..n-1` — from the top of scrollback.
- **Fix:** correct the JSDoc and the five rows. Both documents already contain prose stating the comment is wrong, so each file currently contradicts itself; a reader who lands on the table writes an absence assertion that scrollback satisfies.

### D4 — The agent matchers are documented as stricter than they are

_Findings: 08-16 the-dynamic-skills-matcher-proves-reach-not-laziness; 08-01 agent-matchers-subset-check-reads-as-exact._ **Effort: hour.**

- `assertions.md` says `toHaveAgentDynamicSkills` "checks the `<skill_activation_protocol>` body section"; `anti-patterns.md` says preloaded and dynamic "are mutually exclusive". The matcher strips frontmatter and does `body.includes(id)` over the whole body; `hasActivationProtocol` is a separate optional expectation. Under `createE2ESource()` the template renders every skill's content, and no fixture-compiled agent has the protocol section at all — so the matcher proves **reach, not laziness**, and a lazy claim needs `toHaveAgentFrontmatter({ noSkills })` beside it.
- `e2e-infrastructure.md` lists `toHaveAgentFrontmatter(agentName, { name?, description?, model?, tools?, skills?, noSkills? })`. The real type has eleven fields; the omitted `exactSkills` is exactly the one the matcher's own JSDoc directs authors to when `skills` (a subset check) would pass on an agent that preloads everything. The doc actively steers authors to the weaker check.

### D5 — E2E docs describe surfaces, flags, helpers and budgets that no longer exist

_Findings: 08-18 wizard-docs (SearchModal half); 08-08 init-edit-invalid-source-flag; 07-30 e2e-doc-inventories-pin-counts; 07-17 e2e-helper-tests-have-no-runnable-home; 07-29 e2e-grid-focus-unobservable; 07-31 e2e-fixture-smaller-than-production; 08-18 the-fan-out-line-counts-rewrites._ **Effort: 1–2 hours.**

| What the doc says                                                                                                                                               | What the source does                                                                                                                                                                                               | Fix                                                                                                                                                                         |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `page-objects.md` + `e2e-infrastructure.md` document a `SearchModal` "opened from BuildStep… known to be buggy" and `openSearch()` — "Press `/` to open search" | No `useInput` site in `src/cli/components/` binds `/`; `openSearch` has one occurrence in `e2e/` — its own definition. Screen and page object are both unreachable                                                 | Delete `e2e/pages/steps/search-modal.ts` and `BuildStep.openSearch()`, then the four doc rows                                                                               |
| `patterns.md` documents an `it.fails` for "CLI exits 0 with corrupt source"; `test-structure.md` and `page-objects.md` spell `--source`                         | `Init.static flags` declares `marketplace` and `from` only; the path now throws `Local marketplace not found: '<path>'` and a spec asserts it                                                                      | Replace with `--marketplace`, drop the fixed-bug framing and the `noSource` row                                                                                             |
| `e2e/pages/wizards/edit-wizard.ts` JSDoc justifies `launchInProjectShort` by `findSkillGridPosition` needing a clean layout                                     | The method does not exist; the reference doc already says so ("replaced by `focusSkill`'s closed-loop Tab-walk")                                                                                                   | Name `focusSkill` or state the constraint structurally — this comment is the last claim the method exists, and it is load-bearing for a launcher rule                       |
| `standards/e2e/README.md` and `packages/cli/CLAUDE.md`: a tested helper may live in `e2e/helpers/` **or** `src/cli/lib/__tests__/helpers/`                      | No vitest project collects `*.test.ts` under `e2e/helpers/` — a helper test written where the docs point never runs                                                                                                | Name `src/cli/lib/__tests__/helpers/` as the only home, re-exported through `e2e/helpers/test-utils.ts` (`normalizeGlobalConfig`, `writeTestPackageJson` are the exemplars) |
| `packages/cli/CLAUDE.md`: a footer-less `waitForWizardFooter()` "hangs for the full 15s timeout"                                                                | `WIZARD_LOAD: 45_000`. Every other surface was corrected; CLAUDE.md is the last 15s, in the file agents read first                                                                                                 | 45s                                                                                                                                                                         |
| `anti-patterns.md`: `createE2ESource()` writes one stack and **nine** skills                                                                                    | `E2E_SKILLS` has ten; `test-data.md` and `README.md` both say ten                                                                                                                                                  | Fix to ten, and give the count an owner row in `documentation-bible.md` so it stops living in three files                                                                   |
| `e2e/pages/constants.ts`: `PROPAGATED_RECOMPILE_ONE` commented "fans out to one registered project"                                                             | `propagatedRecompileSummary(rewritten, unchanged, failed)` — the sentinel only appears when one project was **rewritten**; a fan-out reaching one and changing nothing prints `0 registered projects, 1 unchanged` | Say "projects rewritten (reached = rewritten + unchanged)"                                                                                                                  |

### D6 — Config and schema shape: four claims the source contradicts

_Findings: 08-16 hand-maintained-json-schema; 08-18 config-docs-named-nine-symbols (splitConfigByScope half); 08-16 the-seed-contract-cannot-carry-half; 08-16 built-in-matrix-defensive-copy._ **Effort: ~1 hour.**

- `src/schemas/project-config.schema.json` declares `installMode` and lists it in `required`. `ProjectConfig` has no such field — `detectInstallation` derives it via `deriveInstallMode`. The schema's own `$comment` says it is hand-maintained with no generator behind it, so nothing else will catch this. Delete the property and the `required` entry.
- `splitConfigByScope`'s JSDoc and its inner comment both say the project partition's `selectedDomains` key "is cleared". The body spreads `...config` and overrides four other keys — the spread carries `selectedDomains` onto the project half, and the global literal's conditional re-set is a no-op. Correcting the comment needs no behaviour decision; changing the body is a separate call, and nothing observable moves either way.
- `seed-contract.md`: "`MODEL_NAMES` also carries `"inherit"`; the wire has no such member because absence already means keep the default". Source says the opposite — absence resolves to the agent's own `metadata.yaml` model (19 agents declare one), `"inherit"` means the parent's, and `unnameableModelMessage` refuses the value outright. The same document describes that refusal 650 lines later.
- `skills-and-matrix.md` step 7: "Default source (and not devMode): uses pre-computed `BUILT_IN_MATRIX`". `resolveBaseResult` returns `copyOfBuiltInMatrix()` — a per-load copy of `skills`, `categories`, `suggestedStacks` and both `slugMap` records. The copy is the whole point (the local-skill merge writes into `slugMap` via `claimSlug`), and a new mutable collection added to `MergedSkillsMatrix` must join that helper in the same change.

### D7 — Four descriptions of mechanisms that were deleted or never held

_Findings: 08-09 allowdefaultproject-matching; 08-08 a-consumers-table-names-five-of-ten; 08-09 inits-wizard-cannot-see-a-global-roster; 08-18 a-harness-variable-in-the-spawned-binary._ **Effort: minutes.**

- `code-generation.md` explains ESLint's `scripts/` coverage via `parserOptions.projectService.allowDefaultProject`. There is no such key in the config — the only hit is a comment describing the carve-out in the past tense. `packages/cli/scripts/tsconfig.json` replaced it because the glob "matched under bun and failed it under node". Rewrite the bullet to name the discovered project.
- `concepts/scope-system.md` § Scope Predicates: the **Consumers** table names five modules; eleven non-test files import from `scope-predicates` today. One listed row is also wrong — `config-types-writer.ts` imports `activeAgentNames` as well as `activeProjectAgentNames`. Complete the table or retitle it to name the subset.
- `commands/index.md` `init` step 2: "`loadGlobalConfigIfExists()` loads global config to pre-hydrate the wizard". Step 1's dashboard divert returns on the same condition (`detectGlobalInstallation()` succeeding), so the hydration branch can never run. Say so, and say that `edit` is what hydrates a saved roster.
- `src/cli/utils/logger.ts`: `/** When true, suppresses this warning in test environments (VITEST=true). */`. Both E2E runners set `VITEST: undefined` for the child, so an E2E run of the real binary is a test environment in which the warning is **not** suppressed — the comment is false for exactly the runs it is read against. Say "unit run (the process evaluating the gate)".

### D8 — Bookkeeping that misroutes readers

_Findings: 08-18 a-membership-list-nobody-registered; 08-16 a-landed-defect-kept-a-second-tracker-row; 08-01 unused-bindings-in-tests (comment half)._ **Effort: minutes.**

- `architecture-overview.md`'s `related:` points at `reference/architecture/{dependency-graph,boundary-map}.md` — 14-line stubs redirecting to the real bodies one directory up. Repoint to `reference/dependency-graph.md` and `reference/boundary-map.md`.
- `todo/cli.md` still carries the `#### CLI-386` detail section. Its table row was deleted and `archive.md` records "CLI-386 retired unbuilt" on 08-16; all three claims in the surviving section are now false (`PluginManifest` has `category?`, the generator spreads it, the named spec is a plain `it`). Delete the section.
- `src/cli/lib/operations/project/load-agent-defs.test.ts`: comments claim "Source overrides CLI for 'web-developer'" and "CLI-only agent preserved". `loadMergedAgents` is mocked and `loadAgentDefs` only forwards it — the override happened in the fixture literal. Delete or reword; they advertise merge-precedence coverage the spec design cannot express.

---

## 2. Mechanical wins

Ordered by what each catches ÷ effort.

**M1 — Add `ts.isSatisfiesExpression` to `unwrap()` in `scripts/check-enumeration-drift.ts`, then register the lists it unblocks.** _(hour; findings: 08-18 config-docs-named-nine-symbols, 08-18 a-membership-list-nobody-registered.)_
One arm on one function. `as const satisfies` is the house style for a vocabulary array, so the shapes the checker cannot read are precisely the ones worth reading: `SKILL_IDS`, `SKILL_SLUGS`, `CANONICAL_FIELD_ORDER`, `WIZARD_STEP_ORDER`, `INSTALL_MODES`. Three comment blocks in the script already record lists parked for this reason, and `type-system.md` documents the limitation in prose. **Prevents:** every future membership drift in those lists, in place of hand-checking.

**M2 — One pass on `documentation-bible.md`: nine rules, each carrying its own grep.** _(hour; findings: 08-18 a-renamed-builder-dangles-in-four-docs + 08-18 a-renamed-config-field-drifted-through-seven — merge into one old-name rule; 08-01 reference-docs-name-identifiers; 08-08 six-docs-place-the-cross-scope-masking-helpers; 08-17 exhaustive-tables-drift; 07-30 negative-exhaustiveness-claims; 08-08 a-four-outcome-table; 08-01 import-graph-docs-validate-rows; 08-17 a-plans-line-numbers-rot; 08-05 default-stacks-has-no-doc-hook; 08-18 a-finding-quoting-editor-code.)_
The hook table routes by source **directory**, so a rename or deletion inside an already-hooked directory produces no signal — which is how D1, D5 and D7 happened. Rules to write: grep `.ai-docs/` for a renamed or deleted symbol's **old** name before the change is done; identifier diff (every backticked identifier in prose must resolve to a declaration); a symbol paired with a file must be _declared_ in that file; exhaustive tables get a row diff, not just a heading diff; "never called / the only N call sites" claims carry the grep that verifies them inline; a generalisation over a set ("all three") is a cardinality claim; dead-code removal is a re-validation trigger; the line-number ban extends to `todo/plans/` and to `(N lines)` annotations; a `default-stacks.ts` → `built-in-catalogue.md` row; `<!-- prettier-ignore -->` on code blocks quoting non-`packages/cli` workspaces. **Prevents:** the recurring class that fills section 1 — and unlike prose rules, most of these ship as a runnable grep.

**M3 — Reserve a scratch prefix in `packages/cli/.gitignore` and `globalIgnores`, and say why beside `files: TYPESCRIPT_SOURCES`.** _(minutes; finding: 08-08 an-untracked-scratch-directory.)_
A `.ts` file outside the three source trees gets the **JavaScript** parser, not a skip — so it reports "Unexpected token :" against a file whose syntax is fine, and fails lint for the whole package. **Prevents:** an agent losing a session to a gate failure whose error message points nowhere.

**M4 — One pass on `standards/e2e/assertions.md` and `anti-patterns.md`: the assertion rules that were proven and never written.** _(1–2 hours; findings: 08-09 a-removal-reason-asserted-unanchored; 08-16 doctor-specs-asserted-the-blanket-skip; 08-06 e2e-selected-agent-name-assertion-cannot-fail; 08-09 two-real-marketplace-suites; 08-08 two-compile-specs-pin-behaviour; 08-09 flag-acceptance-tests; 07-20 setup-owned-state; 08-08 init-edit-invalid-source-flag; 07-20 page-object-speculative-api; 07-19 aggressive-regex-corrupts-fixtures; 07-30 symlinked-project-path; 07-20 fixture-category-literals; 07-18 live-in-session-selected-state; 07-30 domain-deselect + 07-17 d227-preselect — merge as one reachability rule; 08-01 local-extractor-in-e2e-spec; 07-20 config-text-line-scanner; 07-20 config-load-null-fallback; 07-20 migration-path-missing-marketplace-precondition; 07-30 e2e-helper-extraction-threshold; 08-08 eight-specs-pinned-two-behaviours; 07-20 step-text-constants; 08-07 a-vacuous-guard-and-a-real-one.)_
Roughly twenty rules, each with a shipped worked example. The highest-value ones: a fragment is not a line (anchor a positive `toContain` to something only the intended line carries); assert the row, not the rule; a negative assertion names the input that makes it true, and a negative over tool output quotes the tool's own string; an expected value is derived, never observed; assert the declaration line of a generated artifact, never the bare identifier; a spec named for a source asserts data that source ships; prove the surface is reachable before writing the spec; when a pre-fix binary emits identical bytes, mutate the **fixture** rather than reverting `src/`; a spec pinning a scope or ownership boundary cites the ruling it came from. **Prevents:** the "green proves nothing" class, which produced a third of section 1 and most of the vacuous assertions in M11.

**M5 — Override the `page` fixture in `apps/editor/e2e/fixtures.ts` to route `api.github.com` / `raw.githubusercontent.com` and fail loudly on any un-stubbed URL.** _(hour; finding: 08-18 an-editor-e2e-test-reached-live-github.)_
The file is 21 lines with one `configure` fixture and no `page.route` anywhere. The counter-example is in the same workspace: `apps/editor/vitest.config.ts` wires MSW into every unit file because a guarantee "is only a guarantee if it holds where nobody thought to ask". **Prevents:** every future spec that silently depends on a third party being up — and on their file sizes.

**M6 — Roster-invariant test: export `DOMAIN_AGENTS` and `BUILT_IN_AGENT_GROUPS`, assert both against `AGENT_NAMES`.** _(hour; finding: 08-05 builtin-agent-rosters-unbound.)_
Four surfaces agree today and nothing checks that they do. `tsc` catches a **removed** agent (the name leaves the union) and nothing when one is **added** — which is how six agents once shipped unreachable through the wizard. `agent-system.md` currently owns "`convention-keeper` is the only member with no grid row" in prose; a commented allowlist in the test should own it instead.

**M7 — Route doctor's cross-reference findings to the file that holds the defect.** _(hour; finding: 08-16 every-cross-reference-finding-is-filed-against-the-categories-file.)_
`toSourceIssue` hard-codes `file: SKILL_CATEGORIES_PATH` at all three sites, for all six health findings. `rule-unresolved-slug` exists to point a marketplace author at a typo in `config/skill-rules.ts` — it names the slug correctly and then sends them to a file that does not contain it. `SKILL_RULES_PATH` is already imported. Replace with an exhaustive `switch` over `MatrixHealthIssue["finding"]` with a `never` default (the union closed recently, so this now compiles) — **prevents:** a seventh finding kind shipping without anyone saying where its defect lives.

**M8 — `build marketplace` must not write a file its own loader refuses.** _(hour; findings: 08-17 build-marketplace-writes-a-manifest-its-own-loader-refuses; 07-09 marketplace-schema-name-laxer-than-claude-code.)_
`parseAuthor(undefined)` warns and returns `{ name: "" }`; nothing guards it before `generateAndWrite`, and `marketplaceOwnerSchema` requires `min(1)`. Separately, `validateKebabCaseName` runs only on the `--name` override, so a scoped npm name from `package.json` produces a `marketplace.json` that `claude plugin marketplace add` rejects. And `source-loader` catches every `fetchMarketplace` throw as "Marketplace has no marketplace.json", reporting a schema violation as an absent file. Refuse before writing, validate the derived name, stop lying about the cause. Scope the name fix to the command — tightening `marketplaceSchema` would change how third-party marketplaces parse.

**M9 — Export-parity test for `src/cli/utils/__mocks__/logger.ts`.** _(hour; finding: 08-09 logger-manual-mock-omits-log.)_
Four spies against eight exports; the four buffering functions are absent with no recorded reason. Assert every export of `logger.ts` is present except names in an explicit `DELIBERATELY_ABSENT` array. **Prevents:** `TypeError: … is not a function` thrown from inside the code under test, which reads as a product bug.

**M10 — Stop specs reading the developer's machine.** _(1–2 hours; findings: 08-06 in-process-command-tests-run-against-the-real-home; 08-16 unit-specs-read-the-ambient-config-on-disk.)_
14 of 21 specs under `src/cli/lib/__tests__/commands/` run `Command.run` with no `HOME` stub. `config.test.ts`'s `resolveBranding` describe walks the ladder to the real `~/.claude-src/config.ts` while its `resolveSource` sibling in the same file already saves and replaces `HOME`. `source-loader.test.ts` reads this checkout's own gitignored config. Add the stubs, isolate the source root, and write "an in-process command spec owns its `HOME`, not just its `cwd`" into `test-data.md`.

**M11 — Delete the assertions that cannot fail.** _(1–2 hours; findings: 08-09 flag-acceptance-tests; 07-20 e2e-config-load-null-check + 07-20 config-load-null-fallback — one surviving site, merge; 07-20 e2e-regex-config-extractors; 07-20 config-text-line-scanner; 07-20 matcher-augmentation-inline-shape; 07-20 e2e-unretirable-extractors; 08-16 a-repointed-copy-pin.)_
Twenty `not.toContain("unknown flag")` assertions against a string oclif never prints (it prints `Nonexistent flag:`) — they hold for accepted, rejected, crashed and undefined alike. One remaining `loadProjectConfigFromDir` + `?? []` in a spec file, after which the "only helpers may call it" grep invariant becomes enforceable. Two regex extractors (`uninstall-reinit-lifecycle`, `scope-toggle-combined`) whose imprecision cancels out across the comparison, with `loadConfigOrFail` and `readSkillEntries` already imported in the same files. `toHaveLocalSkills`'s augmentation still declares `string[]` while the implementation takes `readonly string[]`, so a widening already done is inert and `uninstall-assertions.ts` carries a blocker comment for it. Plus one `as unknown as string` and one constant whose name still says `LOCAL_SOURCE_NOT_FOUND` while its value says marketplace.

**M12 — Reap the dead exports, and add the spec that keeps them dead.** _(1–2 hours; findings: 08-07 message-constants-outlive-the-command; 08-17 catalog-emission-blocked; 07-20 single-scope-path-reported; 08-09 e2e-source-seams; 07-20 invalid-by-design-metadata-fixture; 07-31 two-minimum-terminal-height.)_
A spec beside `messages.test.ts` failing on any exported message key with no consumer (the repo already has this shape twice) turns `NO_SKILLS_FOUND`, `VALIDATION_FAILED` and `UPDATING_PLUGIN_SKILLS` red — the first is actively misleading, since three commands print their own inline version. Then delete: `loadAndMergeSkillsMatrix` (zero callers, and the docs already say "Dead. Do not call it"), `InstallationInfo.skillsDir` (written, never read — the exact defect `agentDirs` was converted away from), `seedDefaultSourceCache` (one experimental caller, withdrawn). Re-export `renderUnparseableMetadataYaml` through `test-utils.ts` and repoint the four hand-rolled copies of the same broken-YAML literal. Record in `clean-code-standards.md` § 4 that an unimported constant is a defect, not spare capacity.

**M13 — One pass on `clean-code-standards.md` and `typescript-types-bible.md`.** _(1–2 hours; findings: 07-20 field-name-meaning-mismatch; 07-19 post-construction-conditional-mutation; 07-20 single-scope-path; 08-17 the-stale-id-refusal; 07-31 a-precondition-checked-once; 08-16 a-parse-boundary-followed-by-safeparse; 07-20 command-delegation; 08-17 a-second-producer; 08-08 four-commands-each-carried + 08-07 a-user-facing-report — merge; 07-20 scope-authority-must-follow-work; 08-16 a-test-comment-asserted-os-homedir; 08-09 the-generated-matrix-took-its-key-order; 08-16 two-standing-never-rules; 08-06 matrix-schema-typed-every-id; 08-07 project-config-declares-required-fields; 08-07 zod-optional-accepts-explicit-undefined; 08-17 find-on-a-discriminated-result; 07-20 as-const-satisfies-on-object-with-getter; 08-08 the-selection-validator.)_
Highest-value entries: field names must match field contents (already recurred — tests putting URLs in `ProjectConfig.marketplace`, which holds a name); a function whose result is `JSON.stringify`d builds one literal with conditional spreads, because insertion order _is_ the emitted byte layout; a refusal must not diagnose a cause it cannot observe; a precondition that can change mid-process is re-derived where consumed; a line two commands print belongs on `BaseCommand`; validate ids with `z.enum` over the generated tuple, not `z.string()` (thirteen read-model casts were paying for that); `.exactOptional()` at parse boundaries, since `.optional()` accepts an explicit `undefined` — 137 uses in `schemas.ts` and zero mentions in `.ai-docs/`; `satisfies` must never change the type of what it annotates (put it on member arrays, not an object containing a getter — this one has already bitten twice); no `.find()` on an array of discriminated results. Also: widen the slug rule in both `CLAUDE.md` and 15.2 to `slug, domain or category`, and to test factories, since that is where both breaches lived; and swap `generate-matrix-package.ts`'s `localeCompare` for the byte-wise comparator its sibling generator already uses on committed output.

**M14 — One pass on `packages/cli/CLAUDE.md` and `.ai-docs/agent-findings/{README,TEMPLATE}.md`.** _(45 min; findings: 08-10 a-known-gap-pinned-as-an-arity-assertion; 08-05 roster-expectations-pinned-by-count; 07-20 filesystem-listings-must-print-on-disk-names; 08-01 as-any-on-valid-union-members; 08-01 unused-bindings-in-tests; 07-20 shared-mutable-constants; 07-30 finding-proposed-standard-contradicted-a-never-rule; 08-08 a-findings-two-specs-was-a-sample; 08-07 a-rename-sweeps-affected-files; 07-20 e2e-session-ownership (partial-enum half); 08-01 link-integrity-scan.)_
CLAUDE.md is the file every agent reads first, so its rules propagate fastest: an assertion that passes **because** of a known gap must name the gap (a full `toStrictEqual` against a named constant, never an arity or a length); never pin a roster with `toHaveLength(n)`; never print a `displayName` inside a filesystem-listing block; `as any` joins the banned casts on union members; an unused binding in a test is triage before it is a deletion. On the findings side: a Proposed Standard is a proposal, cross-check it against the NEVER list; say whether a count is a census or a sample; `affected_files:` is the pasted output of the grep that produced the numbers; `status: partial` must cover either half being pending (three findings already use it in the direction the definition excludes); and widen the cross-link check to any frontmatter value naming a findings file — five such targets do not exist today, and `check-findings-frontmatter.ts` only proves the YAML parses.

**M15 — Reword the six built-in agent partials that name this repository's paths.** _(hour; finding: 08-05 built-in-agent-partials-instruct-users.)_
`api-tester` and `ai-developer` partials tell a user's agents to write findings to `.ai-docs/agent-findings/` using this repo's template, and cite "per CLAUDE.md". These are product content shipped to other people's projects. Both exemplars for the fix are in tree (`ai-tester` names no findings path; `reviewer/playbook.md` was already softened to "the way this project's conventions direct"). Add the rule and its grep — `grep -rn "ai-docs\|CLAUDE\.md" src/agents/ --exclude-dir=meta` — to `prompt-bible.md` § 8.

**M16 — Sweep the fifteen `identity.md` files that still open "be comprehensive and thorough".** _(half-day; finding: 08-06 comprehensive-and-thorough-is-mandated.)_
The mandate is gone from both its sources — `agent-summoner` no longer emits it and `prompt-bible.md` now states the opposite ("apply an expansion modifier where the model demonstrably under-delivers, and nowhere else"). `planning/pm/identity.md` carries the replacement voice ("A spec's size follows the task's size, not the template's"). Fifteen shipped prompts contradict the standard that governs them. **This is the single largest lever on your second goal** — it is the instruction that makes every agent here over-produce.

**M17 — Give `generate:types` and `generate:schemas` the self-contained `--check` that `generate:matrix` already has.** _(half-day; findings: 08-06 generated-drift-checks-use-git-diff; 08-07 two-generated-artifacts-were-already-stale.)_
Both are `bun run generate:X && git diff --exit-code`, both are in `prepublishOnly`, and both are unrunnable by an agent under the no-write-git rule — worse, `git diff` on a curated working tree answers a different question than "is the output current", and is blind to newly emitted untracked files. Emit into memory, compare bytes, list drifted paths, exit non-zero. Record in `commit-protocol.md` that a gate an agent must run cannot depend on git state.

**M18 — Small truthfulness fixes in source.** _(30 min total; findings: 07-19 e2e-dual-scope-contention-flake; 08-01 unused-catch-binding.)_
`ensureMarketplace`'s lazy-resolution `catch` discards the error and returns `{ marketplace: null }`, which downstream becomes a hard user error indistinguishable from a genuinely absent marketplace — `warn` is already imported in the file. `validateYamlFiles`'s bare `catch {}` emits "Failed to parse YAML" with no cause while the sibling phase in the same file emits the cause; `getErrorMessage` is already imported.

**M19 — Editor and server harness notes.** _(45 min; findings: 08-17 getbyrole-cannot-see-the-dialog; 08-17 two-elements-can-both-be-visible; 08-08 a-store-reported-every-first-visit; 08-08 the-workers-test-pool-dropped-storage-isolation; 08-08 the-editors-vitest-alias-list.)_
`apps/editor/e2e/README.md` has four convention bullets that point the next author straight at three known traps: only the topmost modal is in the accessibility tree (use the sheet locator, re-assert by role after the top closes, and use a DOM count to separate hidden from closed); a floating control needs a live `boundingBox()` relationship against the container it must clear, and `position: fixed` is unavailable inside the `mx-auto max-w-*` layout; a suite that ignores the console is not watching the app it drives — a warning on every page load survived 186 green tests. Separately: record in `apps/server/vitest.config.ts` that pool-workers 0.20 dropped `isolatedStorage` and `fetchMock`, and in `monorepo-layout.md` that a Vite alias matches by **prefix**, so a package with subpath exports needs a trailing-slash entry ordered first — a bare-only alias corrupts imports into `…/src/index.ts/seed` rather than failing.

**M20 — Reference-doc invariants that only exist in one person's head.** _(45 min; findings: 07-18 sourceById-collapse; 07-30 configwriteresult-globalconfigpath; 08-07 two-preload-tests-name-categories; 08-06 skill-id-prefix-lies-about-domain; 07-31 display-lookup-fallbacks; 07-20 two-config-normalisers.)_
`buildCompileAgents` builds an id-keyed `sourceById` map, so a dual-scope pair collapses last-write-wins; the guard is entirely caller-side and undocumented — note it in `tombstone-pattern.md`. `operations-layer.md` needs "result fields must have a producer". `configuration.md` needs "a category id in a test's classification set is a migration surface" (three hand-maintained sets, nothing type-checks an omission). A skill's domain is its **category's** domain, never its id prefix — `meta-config-stack-detect` declares `category: shared-tooling`, and getting it wrong is a 21-agent difference. `component-patterns.md` needs the rule for when `lookup(id)?.name ?? id` is legitimate, plus the note that Ink's error boundary swallows a component-level assert so `expect(() => render(...)).toThrow()` never passes. And `e2e/assertions/` has five modules and zero tests — `normalizeConfigPreservingOrder` needs the case its order-insensitive sibling cannot have: reordered lines must **not** compare equal.

---

## 3. The order I would do them in

1. **D5's `CLAUDE.md` rows (45s, helper home) + M3 (eslint scratch prefix).** Twenty minutes, and both change what every agent does for the rest of the list — one removes a 45-second-vs-15-second false belief in the first file they read, the other removes a gate failure that derails sessions.
2. **M2 (documentation-bible pass).** Do this _before_ any doc sweep: the old-name grep rule is what makes step 3 verifiably complete instead of nominally complete, and the identifier-diff rule is the standing check for D5 and D7.
3. **D1 (`source` → `origin`).** The only entry where the docs produce a rejected config; run it under M2's rule so the grep proves the sweep closed.
4. **D2 (three CLI strings).** Minutes, user-facing, and one of them exits 127 on the command it recommends.
5. **M4 (e2e assertion rules).** Before any test work below it: M11's replacements and M10's isolation both want a written target to point at, and the rules are what stop the next twenty vacuous assertions.
6. **D3 + D4 (`getScreen`, the matchers).** These are the doc entries that cause _silently passing_ tests, and M4 just gave them a home; doing them together means one pass over `assertions.md`/`anti-patterns.md`/`e2e-infrastructure.md`.
7. **D5 remainder + D8.** Delete the dead page object and the `--source` examples while the same three files are open; sweep the bookkeeping.
8. **M1 (`satisfies` in the drift checker).** Best ratio on the list; do it after the doc sweeps so the newly-correct membership tables get bound to source in the same session rather than re-drifting.
9. **M11 + M10 (assertions that cannot fail; specs that read the real machine).** Now cheap — M4 supplies the rules and M11's config-load fix makes the `loadProjectConfigFromDir` grep invariant enforceable the moment the last site moves.
10. **D6 + D7 (config/schema truth, deleted mechanisms).** Ordinary correction work, and D6's `installMode` deletion is safest after M1, which is what would catch the next hand-maintained schema drifting again.
11. **M6 + M7 + M9 (roster invariant, doctor routing, logger mock parity).** Three self-contained hours; M7's exhaustive switch only compiles because its union closed recently, so take the opening.
12. **M8 (build marketplace).** Product defect with a user at the end of it: the command writes a manifest the CLI itself refuses and then misreports why.
13. **M12 + M18 (dead-export reaper, discarded error causes).** M12's message-key spec is what keeps step 12's and step 4's message work from re-accumulating.
14. **M13 + M14 (clean-code / types-bible / CLAUDE.md / findings format).** The bulk of the standards backlog — cheapest per rule, and M14's findings-format rules are what make the _next_ batch of findings arrive census-counted and cross-checked, which is directly your second goal.
15. **M15 + M16 (agent partials, "comprehensive and thorough").** Do them last as a pair — both are prompt-content sweeps, both ship to users, and M16 is the biggest single change to how much output your agents produce.
16. **M5 + M19 + M20 (editor network guard, harness notes, reference invariants).** Independent of everything above; slot wherever the editor/server work happens.
17. **M17 (git-free `--check`).** Half a day, no dependants, and the only reason to defer it is that it costs the most per class caught — but until it lands, two of your three staleness gates are unrunnable by any agent, so do not defer it twice.

---

## 4. Total effort

- **Section 1 (wrong docs):** 6–9 hours. D1, D5 and D6 are most of it; D2, D3, D7 and D8 are about 90 minutes combined.
- **Section 2 (mechanical):** 24–34 hours, of which M16 and M17 are ~8 on their own.
- **Total: 30–43 hours — four to six focused days.**

Steps 1–8 of the sequence are **8–10 hours** and carry most of the value: every entry that misleads a user or produces a silently-passing test, plus the two checks (M2's greps, M1's drift arm) that stop the class from refilling. Steps 9–14 are the long tail — real, but each one prevents a repeat rather than fixing a live wrong. If you only have the next hour, do steps 1, 2 and 4.

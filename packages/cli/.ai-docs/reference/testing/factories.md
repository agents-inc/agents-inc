---
scope: reference
area: testing
keywords:
  [
    factories,
    createMockSkill,
    buildProjectConfig,
    createMockMatrix,
    helpers,
    assertions,
    content-generators,
    expected-values,
  ]
related:
  - reference/testing/infrastructure.md
  - reference/testing/mock-data.md
last_validated: 2026-09-03
---

# Test Factories & Helpers

> **Split from:** `reference/test-infrastructure.md`. See also: [infrastructure.md](./infrastructure.md), [mock-data.md](./mock-data.md), [e2e-infrastructure.md](./e2e-infrastructure.md).

## Test Utilities (Domain-Scoped Directories)

Test utilities live in three domain-scoped directories under `src/cli/lib/__tests__/`. Each has a barrel `index.ts` for imports. Tests import from `factories/`, `helpers/`, or `assertions/` as needed.

**This document owns the factory, helper and assertion ROSTERS**, and each table below is exhaustive — a symbol absent from it is not exported from that directory. **It states no total, and that is deliberate — do not add one.** A directory count is not bindable here while the rosters below it are, so a total and a roster would sit side by side reading alike while only the roster could ever go red. A total also answers less than the roster under it: two lists agree on a total and disagree on every name in it. The tables are the whole claim, and a reader who wants a number counts their rows.

Re-derive membership from source rather than carrying it forward.
`scripts/check-enumeration-drift.ts` binds all three tables — factories, helpers and assertions —
reading each DIRECTORY rather than its barrel. A barrel is not the claim, and binding one would
report real members as drift (`factories/index.ts` re-exports a strict subset of what its directory
declares; the helper table names `extractNamedSection` and `extractScopeSections`, which no barrel
re-exports at all). Its directory reader takes each module's own declarations plus every name it
re-exports by name, over every non-spec module in the tree, and dedupes. The single-file inventories
further down (FS utilities, expected values, content generators, the journey-page reader) are
registered too.

## Factory Functions (`src/cli/lib/__tests__/factories/`)

**MANDATORY: All test data must use these factories. Never construct inline.**

Barrel import: `import { createMockSkill, buildProjectConfig } from "../__tests__/factories/index.js"`

| Factory                                  | File                    | Purpose                                                                                                                                                                                                                                                                  | Signature                                              |
| ---------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------ |
| `createMockSkill()`                      | `skill-factories.ts`    | Create a ResolvedSkill mock                                                                                                                                                                                                                                              | `(id, overrides?) => ResolvedSkill`                    |
| `createMockExtractedSkill()`             | `skill-factories.ts`    | Create ExtractedSkillMetadata                                                                                                                                                                                                                                            | `(id, overrides?) => ExtractedSkillMetadata`           |
| `createMockSkillEntry()`                 | `skill-factories.ts`    | Create a Skill entry                                                                                                                                                                                                                                                     | `(id, preloaded?, overrides?) => Skill`                |
| `createMockSkillDefinition()`            | `skill-factories.ts`    | Create a SkillDefinition mock                                                                                                                                                                                                                                            | `(id, overrides?) => SkillDefinition`                  |
| `createMockSkillAssignment()`            | `skill-factories.ts`    | Create a SkillAssignment mock                                                                                                                                                                                                                                            | `(id, preloaded?) => SkillAssignment`                  |
| `sa()`                                   | `skill-factories.ts`    | Terse alias of the above                                                                                                                                                                                                                                                 | `(id, preloaded?) => SkillAssignment`                  |
| `saUnflagged()`                          | `skill-factories.ts`    | An assignment stating only WHICH skill — the built-in stacks' shape, and what a saved config carries for a lazy skill. Distinct from `sa(id)`, which writes `preloaded: false` and is therefore somebody's word for the load                                             | `(id) => SkillAssignment`                              |
| `createMockCopiedSkill()`                | `skill-factories.ts`    | Create a CopiedSkill record mock                                                                                                                                                                                                                                         | `(id, overrides?) => CopiedSkill`                      |
| `createMockMultiSourceSkill()`           | `skill-factories.ts`    | Create multi-source ResolvedSkill                                                                                                                                                                                                                                        | `(id, sources, overrides?) => ResolvedSkill`           |
| `createMockSkillSource()`                | `skill-factories.ts`    | Create a SkillSource mock                                                                                                                                                                                                                                                | `(type, overrides?) => SkillSource`                    |
| `createTestSkill()`                      | `skill-factories.ts`    | Create a TestSkill for disk tests                                                                                                                                                                                                                                        | `(id, description, overrides?) => TestSkill`           |
| `testSkillToResolvedSkill()`             | `skill-factories.ts`    | Convert TestSkill to ResolvedSkill                                                                                                                                                                                                                                       | `(skill, overrides?) => ResolvedSkill`                 |
| `buildAgentDefs()`                       | `agent-factories.ts`    | What `loadAgentDefs` answers — the CLI's own sub-agent roster AND the sourcePath its partials sit at, which travel together in production                                                                                                                                | `(agents, sourcePath?) => AgentDefs`                   |
| `createMockAgent()`                      | `agent-factories.ts`    | Create an AgentDefinition mock                                                                                                                                                                                                                                           | `(name, overrides?) => AgentDefinition`                |
| `createMockAgentConfig()`                | `agent-factories.ts`    | Create an AgentConfig mock                                                                                                                                                                                                                                               | `(name, skills?, overrides?) => AgentConfig`           |
| `createMockCompiledAgentData()`          | `agent-factories.ts`    | Create CompiledAgentData mock                                                                                                                                                                                                                                            | `(overrides?) => CompiledAgentData`                    |
| `createMockMatrix()`                     | `matrix-factories.ts`   | Create a MergedSkillsMatrix mock                                                                                                                                                                                                                                         | `(...skills) => MergedSkillsMatrix`                    |
| `createMatrixFromTestSkills()`           | `matrix-factories.ts`   | Build matrix from TestSkill array                                                                                                                                                                                                                                        | `(skills, toResolvedOverrides?) => MergedSkillsMatrix` |
| `buildCategoryMap()`                     | `matrix-factories.ts`   | Widen partial category defs to map                                                                                                                                                                                                                                       | `(defs) => Record<Category, CategoryDefinition>`       |
| `createComprehensiveMatrix()`            | `matrix-factories.ts`   | Full matrix with 8 skills + stacks                                                                                                                                                                                                                                       | `(overrides?) => MergedSkillsMatrix`                   |
| `createBasicMatrix()`                    | `matrix-factories.ts`   | Minimal matrix with 5 skills                                                                                                                                                                                                                                             | `(overrides?) => MergedSkillsMatrix`                   |
| `createMockMatrixConfig()`               | `matrix-factories.ts`   | Create decomposed matrix config                                                                                                                                                                                                                                          | `(categories, overrides?) => MockMatrixConfig`         |
| `createMockCategory()`                   | `category-factories.ts` | Create a CategoryDefinition mock                                                                                                                                                                                                                                         | `(id, displayName, overrides?) => CategoryDefinition`  |
| `buildSourceConfig()`                    | `config-factories.ts`   | Create source config object                                                                                                                                                                                                                                              | `(overrides?) => Record<string, unknown>`              |
| `buildProjectConfig()`                   | `config-factories.ts`   | Create a ProjectConfig mock                                                                                                                                                                                                                                              | `(overrides?) => ProjectConfig`                        |
| `buildWizardResult()`                    | `config-factories.ts`   | Create a WizardResultV2 mock                                                                                                                                                                                                                                             | `(skills, overrides?) => WizardResultV2`               |
| `buildAgentConfigs()`                    | `config-factories.ts`   | Create AgentScopeConfig array                                                                                                                                                                                                                                            | `(agentNames, overrides?) => AgentScopeConfig[]`       |
| `buildSourceResult()`                    | `config-factories.ts`   | Create a SourceLoadResult mock                                                                                                                                                                                                                                           | `(matrix, sourcePath, overrides?) => SourceLoadResult` |
| `initMatrixAndSource()`                  | `config-factories.ts`   | initializeMatrix + buildSourceResult                                                                                                                                                                                                                                     | `(matrix, sourcePath, overrides?) => SourceLoadResult` |
| `buildTestProjectConfig()`               | `config-factories.ts`   | Create TestProjectConfig                                                                                                                                                                                                                                                 | `(agents, skills, overrides?) => TestProjectConfig`    |
| `buildPreRenameProjectConfig()`          | `config-factories.ts`   | A saved config whose project-level source ref still sits under its pre-rename field name — deliberately-invalid parse-boundary data the loader must REFUSE. The ref is not the default public one, so a silent repoint to `DEFAULT_SOURCE` cannot read as a correct load | `() => Record<string, unknown>`                        |
| `buildPreRenameSkillEntryConfig()`       | `config-factories.ts`   | The same one level down: a skill entry carrying its provenance under the old field name. Spelled out rather than built from `buildSkillConfigs`, which follows the type and so stops producing the old shape                                                             | `() => Record<string, unknown>`                        |
| `buildGateReport()`                      | `config-factories.ts`   | The report a gated config write returns. Defaults to a write that changed the global pair and reached nobody; `propagatedTo` produces a fan-out whose projects all report their agents rewritten                                                                         | `(propagatedTo?, overrides?) => GateReport`            |
| `buildSeedSkill()`                       | `seed-factories.ts`     | One shared-configuration skill entry. `install: "eject"` by default — a test source is local and has no marketplace, so plugin mode legitimately refuses it                                                                                                              | `(overrides?) => SeedSkill`                            |
| `buildSeedPayload()`                     | `seed-factories.ts`     | A shared configuration as the web app builds it. Sparse by default — presence is selection                                                                                                                                                                               | `(overrides?) => SeedPayload`                          |
| `buildSeedExternalSkill()`               | `seed-factories.ts`     | One skill the payload CARRIES rather than names, whole directory inline. `categoryId` is a REAL category — the placement the user confirmed against the catalogue the payload names                                                                                      | `(overrides?) => SeedExternalSkill`                    |
| `UPSTREAM_SKILL_NAME`                    | `seed-factories.ts`     | `"brainstorming"` — the upstream name that fixture forks from. A constant, not a factory, and exported from the same module                                                                                                                                              | `string`                                               |
| `createMockResolvedStack()`              | `stack-factories.ts`    | Create a ResolvedStack mock                                                                                                                                                                                                                                              | `(id, name, overrides?) => ResolvedStack`              |
| `createMockStack()`                      | `stack-factories.ts`    | Create a Stack mock                                                                                                                                                                                                                                                      | `(id, config) => Stack`                                |
| `createMockRawStacksConfig()`            | `stack-factories.ts`    | Create raw stacks config (2-stack)                                                                                                                                                                                                                                       | `() => RawStacksConfig`                                |
| `createMockRawStacksConfigWithArrays()`  | `stack-factories.ts`    | Raw stacks with array categories                                                                                                                                                                                                                                         | `() => RawStacksConfig`                                |
| `createMockRawStacksConfigWithObjects()` | `stack-factories.ts`    | Raw stacks with object assignments                                                                                                                                                                                                                                       | `() => RawStacksConfig`                                |
| `createMockCompileConfig()`              | `plugin-factories.ts`   | Create a CompileConfig mock                                                                                                                                                                                                                                              | `(agents, overrides?) => CompileConfig`                |
| `createMockMarketplace()`                | `plugin-factories.ts`   | Create a Marketplace mock                                                                                                                                                                                                                                                | `(plugins?) => Marketplace`                            |
| `createMockMarketplacePlugin()`          | `plugin-factories.ts`   | Create a MarketplacePlugin mock                                                                                                                                                                                                                                          | `(name, source?, category?) => MarketplacePlugin`      |

**`createTestSkill()` / `createMockSkill()` taxonomy contract:** neither reads domain, category or
slug off the ID's segments. Both look the ID up in the canonical taxonomy table at the top of
`skill-factories.ts`, where every value is stated per ID, and overrides win over what is found
there. An ID in neither place THROWS, naming the fields to pass — `createTestSkill` wants
`{ domain, category, slug }`, `createMockSkill` wants `{ category, slug }`. A novel ID therefore
carries the taxonomy its author gave it or fails loudly; it can never carry a plausible-looking one
nobody wrote. `displayName` follows from the slug that resolution produced, so stating a slug
retitles the skill with it.

## Helper Functions (`src/cli/lib/__tests__/helpers/`)

Barrel import: `import { runCliCommand, writeTestSkill } from "../__tests__/helpers/index.js"`. The barrel is a convenience rather than the surface, and it re-exports well under half of what the directory holds — a helper it does not carry is imported from the module the File column names, as `extractNamedSection` / `extractScopeSections` are from `config-source-sections.js` and `toastProximityIn` is from `toast-proximity.js`.

| Helper                                | File                             | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------- | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_ROOT`                            | `cli-runner.ts`                  | Root path constant for CLI commands                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `runCliCommand()`                     | `cli-runner.ts`                  | Run CLI command, capture stdout/stderr/error                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `parseRefusal()`                      | `cli-runner.ts`                  | The refusal oclif's parser prints for a flag a command has not declared, for the spelling given — `Nonexistent flag: <flag>`. Built here because the space after the colon is load-bearing: a site that spells it by hand and loses it negates a string the parser can never print. `e2e/pages/terminal-screen.ts` carries the same string as `PARSE_REFUSAL` for the PTY harness, which cannot import from this tree                                                                                                                                                             |
| `missingArgsRefusal()`                | `cli-runner.ts`                  | The refusal oclif's parser prints when a command is invoked without a positional it declares `required` — `Missing <count> required arg`. The count sits INSIDE the message, so a hand-spelled `missing required arg` is a substring of nothing the parser prints and the negative it feeds can never fail                                                                                                                                                                                                                                                                        |
| `argOptionRefusal()`                  | `cli-runner.ts`                  | The refusal oclif's parser prints for a positional outside the `options` its command declares — `Expected <value> to be one of:`. The argument-side counterpart of `parseRefusal()`, and the only form of the refusal that names WHICH value was rejected. `eject` is the only command declaring `options` today                                                                                                                                                                                                                                                                  |
| `readTestYaml()`                      | `config-io.ts`                   | Read and parse YAML test file                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `readTestJson()`                      | `config-io.ts`                   | Read and parse JSON test file                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `readTestTsConfig()`                  | `config-io.ts`                   | Load TS config file via jiti                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `writeTestTsConfig()`                 | `config-io.ts`                   | Write a config.ts file to a project directory                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `writeTestPackageJson()`              | `config-io.ts`                   | Write package.json (marketplace identity)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `writeCorruptTestConfig()`            | `config-io.ts`                   | Write `source` verbatim as the project's `config.ts` — the raw-text sibling of `writeTestTsConfig`, for the corruption cases a config object cannot express (a syntax error, a missing default export, a shape the loader schema rejects). Returns the written path                                                                                                                                                                                                                                                                                                               |
| `normalizeGlobalConfig()`             | `config-comparison.ts`           | Order-INSENSITIVE config-text normalizer (strips `projects` line, sorts lines)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `normalizeConfigPreservingOrder()`    | `config-comparison.ts`           | Order-SENSITIVE config-text normalizer (strips the same `projects` line, and leaves every remaining line where it was). One substitution away from `normalizeGlobalConfig()` above it and not interchangeable with it: sorting would stop a reordering from failing the comparison, which is the regression class the round-trip caller in `e2e/lifecycle/scope-toggle-roundtrip.e2e.test.ts` exists to catch. Swapping one for the other under an existing assertion is an assertion change, not a refactor                                                                      |
| `writeTestSkill()`                    | `disk-writers.ts`                | Write SKILL.md + metadata.yaml to dir                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `writeSourceSkill()`                  | `disk-writers.ts`                | Write skill to source directory structure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `writeTestAgent()`                    | `disk-writers.ts`                | Write agent metadata.yaml to dir                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `writeSourceAgent()`                  | `disk-writers.ts`                | Write agent to source directory structure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `writeTestInstalledPluginsRegistry()` | `disk-writers.ts`                | Write a claude CLI v2 `installed_plugins.json` under a plugins dir. Takes `Record<"<plugin>@<marketplace>", installPaths[]>` and emits one user-scoped install record per path — the shape `claude plugin install` (>= 2.1.220) writes for its cache layout. Returns the registry path.                                                                                                                                                                                                                                                                                           |
| `writeTestPluginManifest()`           | `disk-writers.ts`                | Write a `.claude-plugin/plugin.json` manifest to dir. Pretty (2-space) by default; pass `{ pretty: false }` for compact-form call sites.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `createTestDirs()`                    | `test-dir-setup.ts`              | Create plugin test directory structure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `cleanupTestDirs()`                   | `test-dir-setup.ts`              | Clean up plugin test directory structure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `setupIsolatedHome()`                 | `isolated-home.ts`               | Isolate cwd + HOME + project dir per test. It ALSO sets `AGENTS_INC_SKIP_NEW_VERSION_CHECK` for the life of the home, but that is no longer where the update-check door is shut — `vitest.setup.ts` pins the same variable at module scope for the whole process, because the variable is process-wide and names no home, so a per-home setting covers only the specs that ASKED for one. What this helper's copy still buys is the restore: `cleanup` puts the variable back as it was found, absent included                                                                    |
| `useFakeHome()`                       | `isolated-home.ts`               | beforeEach/afterEach fake-HOME lifecycle wrapper; sets the same update-check variable, `setHome: false` or not. Same reading as the row above — the door is already shut process-wide by `vitest.setup.ts`, and `src/cli/lib/__tests__/update-check-door-closed-process-wide.test.ts` is what holds it there, from a spec that builds no home and opts into nothing                                                                                                                                                                                                               |
| `ensureSharedMarketplaceCheckout()`   | `shared-marketplace-checkout.ts` | Publishes the DEFAULT public marketplace once per machine at a fixed `os.tmpdir()` path and records it so nothing revalidates it. Called from `vitest.global-setup.ts` only — that is the one point in a run where no worker is racing it, and `shared-fixture-writers.test.ts` holds it to that                                                                                                                                                                                                                                                                                  |
| `ensureRecordedCheckout()`            | `shared-marketplace-checkout.ts` | The mechanism the above is the shared-path call site of, taking the cache home AND the fetch as parameters so a spec can drive the whole thing at a root it created and without a download. The record is written on EVERY call rather than only after a fetch: `rename` publishes the directory, the record is a SIBLING path, and a run killed between the two left a checkout the guard accepted and nothing recorded — which sent every later run back to the network, permanently                                                                                            |
| `linkSharedCache()`                   | `shared-marketplace-checkout.ts` | Symlinks a fake home's `.cache` at that checkout, so a command falling through to the default source finds it instead of fetching from GitHub. Called by both helpers above; throws `EEXIST` on a fake home that already has one, which names the reuse rather than hiding it                                                                                                                                                                                                                                                                                                     |
| `silenceConsole()`                    | `silence-console.ts`             | Suppress console output during a test body                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `useMockWorker()`                     | `mock-worker.ts`                 | Installs `@workspace/api-mocks` for this file through `msw/node` — the one description of the worker `apps/editor`'s suites also read — and returns the requests it saw, in arrival order. `onUnhandledRequest: "error"`, so a request to a route those handlers do not describe fails the test that made it. Not re-exported from `index.ts`, deliberately: that barrel is imported by most of this suite and would pull `msw/node` into every one of those graphs                                                                                                               |
| `buildSkillConfig()`                  | `wizard-simulation.ts`           | Create a single SkillConfig                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `buildSkillConfigs()`                 | `wizard-simulation.ts`           | Create SkillConfig array                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `simulateSkillSelections()`           | `wizard-simulation.ts`           | Simulate user skill selections                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `buildWizardResultFromStore()`        | `wizard-simulation.ts`           | Build WizardResultV2 from store                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `extractSkillIdsFromAssignment()`     | `wizard-simulation.ts`           | Extract IDs from stack assignment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `extractNamedSection()`               | `config-source-sections.ts`      | Extract a named config.ts const block                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `extractScopeSections()`              | `config-source-sections.ts`      | Split a section into global/project parts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `parseCompiledAgentSections()`        | `compiled-agent-sections.ts`     | One compiled agent's own composition, read back out of its text: the parsed frontmatter, the preload list in rendered order and in whichever ref form each entry carries, the activation protocol's entries as heading id paired to `Invoke:` ref, and the top-level sections in render order. The two sides are not symmetrical — a preload renders as `pluginRef ?? id` while a dynamic skill's heading is the bare id in both modes — so an expectation keyed on one form reads one of the four lines a skill can occupy. Reached from E2E through `e2e/helpers/test-utils.ts` |
| `parseTestFrontmatter()`              | `index.ts`                       | Lightweight frontmatter parser for assertions                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `elementAt()`                         | `element-at.ts`                  | The element at `index`, or a failure naming the index and the collection's length. **Tests only** — production code should be arranged so the element's presence is provable                                                                                                                                                                                                                                                                                                                                                                                                      |
| `firstElement()`                      | `element-at.ts`                  | `elementAt(items, 0)`. Same tests-only rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `installThroughOperations()`          | `install-through-operations.ts`  | The install `init` performs once the wizard has answered — copy the selected skills, write the scoped config pair, compile every scope's sub-agents — in the command's order and with its arguments. A spec needing an installed project builds one through here rather than through a sequence of its own                                                                                                                                                                                                                                                                        |
| `readGeneratedUnion()`                | `generated-types.ts`             | Reads one `export type <alias> = …;` body out of generated types source. Returns `undefined` when the alias is absent, so a caller can tell "declared as nothing" from "not declared" — asserting the alias exists is the caller's job                                                                                                                                                                                                                                                                                                                                            |
| `readGeneratedUnionMembers()`         | `generated-types.ts`             | The literal members of one alias, in emission order — the values form, for the comparisons a body cannot answer ("do these two writers name the same set"). THROWS when the alias is undeclared rather than answering `[]`, because `[]` is a real answer: the writer emits `never` for an install that selected nothing and a bare alias reference when it falls back, and spelling absence the same way makes every set comparison hold against a file declaring nothing                                                                                                        |
| `readSpecNames()`                     | `journey-page.ts`                | Every spec under an `e2e/` root, named the way `standards/e2e/user-journeys.md` names one — directory, basename, no `.e2e.test.ts` suffix. The SPECS are derived, never listed: a hand-kept list of them is what let a new file's rows go unjudged in silence. Which DIRECTORIES hold them is a separate question, answered by `SPEC_DIRECTORIES` and deliberately not inferred from this list                                                                                                                                                                                    |
| `readJourneyRows()`                   | `journey-page.ts`                | The journey rows a page carries, read off the page and classified against the spec list given. THROWS when the table shape has moved or a name inside a spec directory answers to no file — a reader returning `[]` makes every judgement over it hold vacuously                                                                                                                                                                                                                                                                                                                  |
| `journeyNumbersIn()`                  | `journey-page.ts`                | Every `#` the journey tables number, whatever shape the row carrying it came out — blind to everything else a row must satisfy, which is what makes it a check ON `readJourneyRows` rather than a second copy of it. A row that reader drops still answers here, and `spec-gates.test.ts` holds the two against each other; without it one unescaped `\|` inside a code span deletes a row from every gate that walks row → spec, in silence                                                                                                                                      |
| `specsNamedBy()`                      | `journey-page.ts`                | The specs one row names that a run can actually be pointed at                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `unlocatedSpecsIn()`                  | `journey-page.ts`                | Every name that resolves to a real spec by BASENAME alone — the page naming proof it gives no way to reach. A page defect, kept apart from the next one because they mean opposite things                                                                                                                                                                                                                                                                                                                                                                                         |
| `nonSpecNamesIn()`                    | `journey-page.ts`                | Every backticked name no spec answers to — helpers, fixtures and code symbols. Legitimate, but each must be justified by name in `spec-gates.test.ts`'s `RECOGNISED_NON_SPEC_NAMES`                                                                                                                                                                                                                                                                                                                                                                                               |
| `SPEC_SUFFIX`                         | `journey-page.ts`                | `".e2e.test.ts"` — the suffix the `e2e` project collects and so the one every journey name carries                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `TO_TEST_MARKER`                      | `journey-page.ts`                | `"TO TEST"` — the marker the coverage vocabulary defines as "no from-scratch spec"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `SPEC_DIRECTORIES`                    | `journey-page.ts`                | The `e2e/` subdirectories that hold specs — STATED, not read off the tree. Derived, a directory's kind was a fact about what happened to sit in it, so the first spec written under `e2e/fixtures/` turned journey 1's `fixtures/dual-scope-helpers.initGlobalWithEject` into a spec reference answering to no file and reddened four gates naming a row and a helper that had not moved                                                                                                                                                                                          |
| `specDirectoriesIn()`                 | `journey-page.ts`                | The `e2e/` subdirectories a spec list actually spans. Exported for the roster gate in `journey-page.test.ts`, which holds `SPEC_DIRECTORIES` against it with `toStrictEqual` — the reader must never consult it, or the coupling above returns                                                                                                                                                                                                                                                                                                                                    |
| `FACTORY_DEFAULT_SCOPE`               | `wizard-simulation.ts`           | The scope a factory-built saved config row carries (`"project"`). Deliberately **not** `DEFAULT_SELECTION_OPTIONS.scope` from `@workspace/matrix`, which answers "what does an untouched _pick_ do?" — a spec asserting what an untouched pick produces must name its scope explicitly                                                                                                                                                                                                                                                                                            |
| `envReadsIn()`                        | `env-reads.ts`                   | Every `process.env` touch in one source, split four ways — variables the source names, identifiers a bracket read goes through, `...process.env` spreads, and occurrences none of those explains. The last is load-bearing: a reader that silently dropped forms it did not know would report a clean roster for a file full of reads it cannot see, and a gate over it would be green for exactly that reason                                                                                                                                                                    |
| `callSiteLines()`                     | `source-call-sites.ts`           | Every line of a source on which a given call appears, trimmed and in source order. One entry per OCCURRENCE and never per line, so two calls sharing a line are two entries — a reader that collapsed them would report a file that grew a second call site as unchanged                                                                                                                                                                                                                                                                                                          |
| `callSiteOwners()`                    | `source-call-sites.ts`           | The class member each occurrence of a given call sits inside, in source order and WITH repeats — two presses in one method are two call sites. `unattributed` carries every occurrence with no member declaration above it, so a shape the reader does not understand is reported rather than dropped: a gate handed a silently-short roster answers clean for a file full of calls it could not see                                                                                                                                                                              |
| `constantMembersNamed()`              | `source-call-sites.ts`           | Every member of a named constant a source references, in source order and WITH repeats, plus a count of references that name no member. Repeats are the point: two sites reaching for one value are two sites, and a set reports the pair and the survivor identically                                                                                                                                                                                                                                                                                                            |
| `invocationsIn()`                     | `handed-out-invocations.ts`      | Every invocation a source hands its reader, read through the `CLI_INVOKE_COMMAND` interpolation and the lowercase words after it. The capture ends at the first thing that is not a command word, which separates the topic-plus-command form from a command with arguments without knowing either roster                                                                                                                                                                                                                                                                         |
| `HANDED_OUT_INVOCATIONS`              | `handed-out-invocations.ts`      | The invocations the CLI's guidance currently hands out, STATED rather than derived, because an E2E spec runs each against the real binary — a scan feeding itself would run whatever the messages happen to say and report success for a message naming nothing. `compile`'s no-skills refusal told stuck users to run an `add` command the CLI has never had                                                                                                                                                                                                                     |
| `agentFieldsReadBy()`                 | `template-field-reads.ts`        | Every field a Liquid template looks up on `agent`, FIRST segment only — `agent.disallowedTools.size` asks the model for `disallowedTools`, and `size` is Liquid's own pseudo-property on the value that comes back rather than any part of a TypeScript type. Only lookups rooted at `agent` are read: the template's top-level context keys fail the same silent way, but `{% for %}` introduces bare identifiers of its own, so a scan for them would report on a loop variable                                                                                                 |
| `partialsRenderedBy()`                | `template-partial-renders.ts`    | Every partial a Liquid template renders, in tag order and named once each, with the `methodologies/` prefix kept as the tag writes it — a tag repointed at another directory is part of what the template asked for. Sibling of `agentFieldsReadBy()` and unreadable by the same four things at once: `tsc` never opens a `.liquid`, ESLint does not lint one, the engine runs `strictVariables: false`, and a `{% render %}` tag that disappears leaves no residue — the compiled sub-agent is simply shorter                                                                    |
| `toastProximityIn()`                  | `toast-proximity.ts`             | Every place a processed-buffer read sits within three lines of one of the toast constants given. Ink rewrites a toast's absolutely-positioned row in place, so the processed buffer holds it only while its frame is current — such a read passes or fails on a race, which is the weakest possible evidence for the claim those assertions exist to make, that a refusal is LOUD rather than a silent no-op                                                                                                                                                                      |
| `frozenHomeConstantsIn()`             | `frozen-home-declarations.ts`    | Every exported constant a source declares whose value is derived from `os.homedir()` — the shape that freezes the home a module was first imported under. A DECLARATION shape rather than a comment scan, because a gate looking for a note saying "read at call time" is satisfied by writing that note above a constant that does not; the span stops at the declaration's own semicolon, so the multi-line `path.join` form is caught and a later statement cannot satisfy it                                                                                                  |
| `SHARED_PATTERN`                      | `kebab-case-judges.ts`           | `"KEBAB_CASE_PATTERN"` — the one constant every aligned kebab-case judge reaches for, named here so the scan and the roster it feeds cannot disagree about which constant that is                                                                                                                                                                                                                                                                                                                                                                                                 |
| `kebabCaseJudgesIn()`                 | `kebab-case-judges.ts`           | Every surface in one source that decides whether a name is kebab-case, named once each. BEHAVIOURAL: each anchored regex literal is asked what it accepts, because two correct spellings of the rule share no substring a scan could key on, and a scan for the shared constant's name sees only the surfaces that already agree. The constant's own declaration is excluded — it judges nothing                                                                                                                                                                                  |
| `paintedToastsIn()`                   | `painted-toasts.ts`              | Every message the product paints into the wizard's toast row, resolved through the constant members the store assigns, plus every toast expression that resolves to no fixed string. The unresolved half is reported rather than dropped: a roster built on top of a silent miss reads exactly like a complete one                                                                                                                                                                                                                                                                |
| `toastKeysIn()`                       | `painted-toasts.ts`              | Which members of a sentinel constant name a toast, answered by matching each member's message EXACTLY against what `paintedToastsIn` finds. Exact rather than by containment — `STEP_TEXT.SCOPE` is a substring of the scope-toggle toast, so containment turns a step heading into a toast                                                                                                                                                                                                                                                                                       |
| `producerRostersIn()`                 | `producer-rosters.ts`            | Every roster of PRODUCERS a spec declares, each with whether the file holds that roster's membership against a walk of the tree. A roster of callables only — a roster of paths or of symbol names reads, in syntax, exactly like the test data a spec is parameterised by                                                                                                                                                                                                                                                                                                        |
| `exportedCallablesIn()`               | `test-only-invocations.ts`       | Every name a module exports that something could CALL — a function declaration and the two function-valued `const` forms, and nothing else. A default export is loaded by path and named by no caller, and a value assembled by a call is a callable no syntax names as one; both are left out rather than guessed at, which under-reports and never over-reports. A barrel finds nothing here, because it declares none of what it forwards                                                                                                                                      |
| `invokedNamesIn()`                    | `test-only-invocations.ts`       | Every name a file reaches as a VALUE, in source order and named once each. A bare name handed to a higher-order function is a call the file makes — `.filter(isSnakeCase)` is how a live function was once reported dead — while a name appearing only in an import list, an export list, a type position, a property key or a doc comment is not, and those are the shapes that reported two dead symbols as reached                                                                                                                                                             |
| `isTestSupportModule()`               | `test-only-invocations.ts`       | Whether a path belongs to the suite rather than to the package the suite is run against: a spec, `__tests__/`, `__mocks__/`, the E2E tree, or one of the runner's own entry points. A test-support module read as production keeps every symbol it touches out of any roster built over it                                                                                                                                                                                                                                                                                        |
| `globalHomeFor()`                     | `global-home.ts`                 | Which directory is the global HOME for an installation rooted at `dir` — `globalHome ?? dir`. One definition because it was two: `CLI.run` decides the HOME the spawned binary runs under and `expectFourSurfaces` decides the HOME it then reads, and they must agree or the assertion inspects a tree the command never wrote.                                                                                                                                                                                                                                                  |
| `flattenCliOutput()`                  | `flatten-cli-output.ts`          | Undoes oclif's terminal wrapping — it breaks error and warning text at the terminal width and prefixes each continuation with `›` — so a spec can assert the WHOLE sentence rather than a fragment that would also pass on a truncated message. Lives here rather than beside its first caller because no vitest project collects `*.test.ts` under `e2e/helpers/`; E2E specs reach it through `test-utils.ts`.                                                                                                                                                                   |
| `compactedStackIn()`                  | `compacted-stack.ts`             | The `const stack` declaration of a CLI-written `config.ts`, parsed from the TEXT as the writer laid it out. Deliberately not a structural load: `compactAssignment` and `compactCategoryAssignments` in `@workspace/compile`'s `config-source.ts` write an unflagged assignment as a bare id and drop an exclusive category's array wrapper, and `normalizeAgentConfig` in `src/cli/lib/stacks/stacks-loader.ts` expands both back on load, so an assertion naming a written form can only see it here.                                                                           |
| `sharedSourcePath()`                  | `shared-source.ts`               | Where the run's two frozen E2E fixtures live — the plugin-capable tree at `fixture/` and the plain one at `plain/`. Derived rather than random, because `globalSetup` builds them in vitest's own process and the specs read them from forked workers, so both sides must compute the location without being handed it.                                                                                                                                                                                                                                                           |
| `isSharedSource()`                    | `shared-source.ts`               | Whether a path IS that root, so `cleanupTempDir` refuses to delete a tree it does not own. A spec handed the shared fixture's `tempDir` still calls cleanup on it, and without this the first such spec would remove the fixture out from under every one scheduled after it.                                                                                                                                                                                                                                                                                                     |
| `buildSharedSource()`                 | `shared-source.ts`               | Builds both fixtures at {@link sharedSourcePath} through the callback it is given, and freezes the result. Called from `e2e/global-setup.ts` only                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `removeSharedSource()`                | `shared-source.ts`               | Teardown for that root. Called from `e2e/global-setup.ts` only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `buildFrozenSourceTree()`             | `shared-source.ts`               | The mechanism the two above are the shared-path call sites of: build a tree at the root it is GIVEN and `chmod -R a-w` it. The freeze is not protecting the fixture — it is protecting the DIAGNOSIS: a spec that writes into a source it does not own fails at that write, in its own file, instead of leaving a mutation for some later spec to trip over under a worker schedule that moves between runs. The root is a parameter because the shared one is machine-wide and a spec that built there deleted the E2E suite's fixture mid-run                                   |
| `removeFrozenSourceTree()`            | `shared-source.ts`               | Removes a tree at the root it is given. Unfreezes first, because a frozen tree refuses its own deletion — and a root left behind by a killed run would otherwise be inherited half-built by the next one                                                                                                                                                                                                                                                                                                                                                                          |
| `entryAt()`                           | `element-at.ts`                  | The value at `key`, or a failure naming the key and the record's own keys. The record equivalent of `elementAt()` above — a `record[key] ?? {}` reads as an absence guard but is satisfied just as well by a key that quietly stopped being populated, so a spec asserting the ABSENCE of fields on that value cannot tell the two cases apart. **Tests only**, same rule as `elementAt()`                                                                                                                                                                                        |
| `offendingLines()`                    | `text-scans.ts`                  | Every line of a text that any of a given set of patterns matches, trimmed, in source order. Extracted out of a spec file rather than left as a local scan, so the loop has its own tests                                                                                                                                                                                                                                                                                                                                                                                          |
| `retiredFormsIn()`                    | `text-scans.ts`                  | Every string in a given list that still appears verbatim somewhere in a text, in the order the list gives them. Sibling of `offendingLines()`, same reason for living here rather than inline                                                                                                                                                                                                                                                                                                                                                                                     |

## Assertion Helpers (`src/cli/lib/__tests__/assertions/`)

Barrel import: `import { expectConfigSkills } from "../__tests__/assertions/index.js"`

| Helper                       | File                   | Purpose                                                                                                                                                                                                                                                                                                                                      |
| ---------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `expectConfigSkills()`       | `config-assertions.ts` | Assert expected skills in config                                                                                                                                                                                                                                                                                                             |
| `expectConfigAgents()`       | `config-assertions.ts` | Assert expected agents in config                                                                                                                                                                                                                                                                                                             |
| `expectSkillConfigs()`       | `config-assertions.ts` | Assert skill config entries                                                                                                                                                                                                                                                                                                                  |
| `expectAgentConfigs()`       | `config-assertions.ts` | Assert agent config entries                                                                                                                                                                                                                                                                                                                  |
| `expectAgentCompilation()`   | `agent-assertions.ts`  | Assert agent was compiled                                                                                                                                                                                                                                                                                                                    |
| `expectValidAgentMarkdown()` | `agent-assertions.ts`  | Assert a compiled agent's structural marks: the opening frontmatter fence, its `name:` and a `description:`, the `<operating_principles>` block the template renders, and the trailing `<system-reminder>`. All are checked unconditionally — an agent legitimately lacking one needs its own named assertion rather than a flag on this one |

## FS Utilities (`src/cli/lib/__tests__/test-fs-utils.ts`)

Exhaustive — nothing else is exported from that module, and `scripts/check-enumeration-drift.ts` holds the row that keeps it so.

| Helper              | Purpose                   |
| ------------------- | ------------------------- |
| `createTempDir()`   | Create temp directory     |
| `cleanupTempDir()`  | Remove temp directory     |
| `fileExists()`      | Check if file exists      |
| `directoryExists()` | Check if directory exists |

## Expected Values (`src/cli/lib/__tests__/expected-values.ts`)

Canonical expected value constants for test assertions. Exhaustive — the module exports these two and nothing else:

| Export            | Purpose                                                                      |
| ----------------- | ---------------------------------------------------------------------------- |
| `EXPECTED_AGENTS` | Agent name lists per domain (WEB, API, CLI, WEB_AND_API, ALL)                |
| `EXPECTED_SKILLS` | Skill ID lists per fixture (WEB_DEFAULT, API_DEFAULT, WEB_AND_API, ALL_TEST) |

## Content Generators (`src/cli/lib/__tests__/content-generators.ts`)

Pure content renderers for test file generation. Exhaustive — no other function is exported from that module, and `scripts/check-enumeration-drift.ts` holds the row that keeps it so:

| Function                          | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `renderSkillMd()`                 | Generate SKILL.md with frontmatter                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `renderMetadataYaml()`            | Generate skill metadata.yaml from `SkillMetadataFields`                                                                                                                                                                                                                                                                                                                                                                                             |
| `renderIncompleteMetadataYaml()`  | Generate a metadata.yaml with the named required fields left out — the shape `compile` refuses and `doctor` reports. Deliberately separate from `renderMetadataYaml()`: incompleteness is asked for by name, never fallen into by omission. `(fields, omitted: readonly RequiredMetadataField[]) => string`                                                                                                                                         |
| `renderUnparseableMetadataYaml()` | Generate a metadata.yaml no YAML parser can read — the other half of the class `renderIncompleteMetadataYaml()` covers. `readSkillMetadata` refuses both ways of describing nothing (a file nothing parses out of, and a file that parses without the required fields), and an error-path fixture asks for either BY NAME rather than hand-rolling a broken string per spec. `() => string`                                                         |
| `renderAgentYaml()`               | Generate agent source metadata.yaml — `(name, description?, options?)`. `description`, `title` and `tools` fall back to stand-ins; `model`, `effort`, `disallowedTools`, `permissionMode`, `isolation`, `hooks` and `experimental` are emitted only when the options bag names them, so a fixture needing one asks for it here rather than hand-rolling YAML. `hooks` and `experimental` are written as JSON-in-YAML, the form `agent.liquid` emits |
| `renderAgentMd()`                 | Generate installed agent markdown (`.claude/agents/*`)                                                                                                                                                                                                                                                                                                                                                                                              |
| `renderConfigTs()`                | Generate config.ts with export                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `renderRulesTs()`                 | Generate config/skill-rules.ts from a `SkillRulesFile` — inferred from `skillRulesFileSchema`, so a fixture supplying a field the rules file no longer carries stops compiling                                                                                                                                                                                                                                                                      |

**`renderMetadataYaml()` contract:** `contentHash` and `category` are the required fields on `SkillMetadataFields`, so every call names them and both always reach the file; `author` is always emitted too (defaults to `@test`). `category` is the one field `localRawMetadataSchema` requires that is never supplied on the caller's behalf — a call omitting it does not compile — so no fixture reaches the `local` placeholder by omission, and a fixture that wants the placeholder names it like any other value. The other three required fields are filled when unnamed: `displayName` and `slug` from fixed stand-ins, `domain` from the named category's `<domain>-` prefix, so a fixture naming a category and no domain gets the domain that category belongs to rather than a constant contradicting it. Fields are written in a fixed order the caller cannot influence — `custom`, `domain`, `author`, `displayName`, `category`, `slug`, `cliDescription`, `usageGuidance`, `contentHash`, `forkedFrom` — and a trailing newline is always appended. A fixture that must omit `contentHash`, drop `author`, or use a different field order cannot use this renderer (the one former carve-out, `outdatedForkMetadata`, went with the `update` rewrite that stopped comparing hashes).

## Test Source Factory (`src/cli/lib/__tests__/fixtures/create-test-source.ts`)

Creates complete project directory structures for integration tests:

```typescript
import { createTestSource } from "../fixtures/create-test-source.js";

const dirs = await createTestSource({
  skills: [...],         // TestSkill[]        — source skills under <sourceDir>/src/skills/
  agents: [...],         // TestAgent[]        — source agents
  stacks: [...],         // TestStack[]        — writes config/stacks.ts
  projectConfig: {...},  // TestProjectConfig  — writes <projectDir>/.claude-src/config.ts
  pluginManifest: {...}, // TestPluginManifest
  localSkills: [...],    // TestSkill[]        — writes into <projectDir>/.claude/skills/
  asPlugin: true,        // build as a plugin structure under .claude/plugins/<plugin-name>
});
```

`TestSourceOptions` field names are load-bearing: the project config option is **`projectConfig`**, not `config`.

`TestDirs` (the return shape) is `{ tempDir, projectDir, sourceDir, skillsDir, agentsDir, pluginDir?, configDir? }`. There is no `root` / `skills` / `agents` / `config` field. `cleanupTestSource(dirs)` tears the whole tree down.

The module also re-exports the file-IO helpers used alongside it: `readTestFile`, `readTestYaml`, `readTestJson`, `writeTestFile`, `writeTestYaml`, `fileExists`, `directoryExists`.

Three marketplace helpers live in the same module, because a fixture source's skill ids are written
inside its marketplace's namespace: `TEST_MARKETPLACE_NAME` (`"test-fixture"`),
`testMarketplaceSkillId(bareId)` which composes one id inside it, and `inTestMarketplace(skills)`
which re-namespaces a whole `TestSkill[]`. `cleanupTestSource(dirs)` tears the tree down.

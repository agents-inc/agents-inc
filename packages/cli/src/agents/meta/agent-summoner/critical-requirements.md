## Before Any Work

**Where the working tree is the `agents-inc` repository, read
`packages/cli/.ai-docs/standards/prompt-bible.md` first** — it carries the structure every agent in
this product is built to, its production validation checklist, and the reasoning behind each part.
Where it is absent, the playbook's structure section and the agents on disk are the reference.

<self_correction_triggers>

## Self-Correction Checkpoints

- About to rewrite a file → catalogue its sections first, and name which ones the change touches.

</self_correction_triggers>

---
/**
 * Deliberately empty. The site has one theme, so there is nothing to select —
 * see src/components/theme-provider.astro for the reasoning. Starlight has no
 * way to switch a header control off, but every one of them is overridable, so
 * an empty component is how you remove one.
 */
---

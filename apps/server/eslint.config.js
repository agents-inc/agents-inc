import { baseConfig, typeCheckedConfig } from "@workspace/eslint-config/base"
import { defineConfig, globalIgnores } from "eslint/config"

export default defineConfig([
  ...baseConfig,
  ...typeCheckedConfig(import.meta.dirname),
  // Generated by `wrangler types`; not ours to lint.
  globalIgnores(["worker-configuration.d.ts"]),
  // `wrangler dev`'s bundled output. Gitignored, but eslint walks it anyway
  // and reports on bundled dependency code.
  globalIgnores([".wrangler/**"]),
])
